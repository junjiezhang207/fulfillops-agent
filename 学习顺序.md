# 项目学习顺序

> 按"从整体到细节、从简单到复杂"的顺序阅读。  
> 每读完一个文件，用一句话总结它解决了什么问题——能做到这一点，面试就没问题了。

---

## 第一轮：看懂系统边界（约 1 小时）

> 目标：搞清楚这个项目是什么、请求从哪里进来、有哪些可调的参数。

| # | 文件 | 重点关注 |
|---|---|---|
| 1 | [README.md](README.md) | 架构图、四种 Agent 模式对比、快速启动 |
| 2 | [app/core/config.py](app/core/config.py) | 所有配置项，理解系统有哪些可调参数（LLM/Embedding/Redis/Milvus） |
| 3 | [app/main.py](app/main.py) | FastAPI 启动入口，看 `TraceIdMiddleware` 怎么注册的 |
| 4 | [app/api/router.py](app/api/router.py) | 路由聚合，看所有端点怎么组织 |
| 5 | [.env.example](.env.example) | 配置示例，理解如何切换 LLM Provider |

---

## 第二轮：看懂数据结构（约 30 分钟）

> 目标：理解系统的"语言"——状态和 Schema 定义了各模块如何传递数据。

| # | 文件 | 重点关注 |
|---|---|---|
| 6 | [app/graph/state.py](app/graph/state.py) | **最重要**，`GraphState` TypedDict + `Annotated[list, add]` reducer 的含义 |
| 7 | [app/schemas/agent.py](app/schemas/agent.py) | `FulfillmentDecision`、`ReActCycle`、`PlanExecuteRequest` 等 Schema |
| 8 | [app/schemas/workflow.py](app/schemas/workflow.py) | `WorkflowRunResult`、`InterruptEvent` 的字段含义 |

---

## 第三轮：看懂 LangGraph Workflow（约 1.5 小时）

> 目标：理解 StateGraph 怎么把节点串起来，以及 HITL 中断/恢复机制。  
> 顺序很重要：先看简单的路由函数，再看节点，最后看图的组装。

| # | 文件 | 重点关注 |
|---|---|---|
| 9 | [app/graph/router.py](app/graph/router.py) | 最简单，10 行，条件边路由函数怎么写 |
| 10 | [app/graph/trace.py](app/graph/trace.py) | `TraceEvent` / `ErrorEvent`，理解节点追踪机制 |
| 11 | [app/graph/risk_evaluator.py](app/graph/risk_evaluator.py) | HITL 多规则风险引擎，四级风险怎么判断 |
| 12 | [app/graph/nodes.py](app/graph/nodes.py) | **重点**，5 个节点实现，看 `Command(goto="finalize")` 错误短路和 `interrupt()` 调用 |
| 13 | [app/graph/workflow.py](app/graph/workflow.py) | 把节点组装成 StateGraph，看条件边 mapping 怎么写 |
| 14 | [app/graph/parallel_workflow.py](app/graph/parallel_workflow.py) | Send API 并行 fan-out/fan-in，`route_after_stage1` 返回 `list[Send]` |

---

## 第四轮：看懂 ReAct Agent（约 1.5 小时）

> 目标：理解工具层的设计，以及 Agent 如何自主决定调用哪些工具。  
> 从最底层（缓存→弹性→工具）往上读。

| # | 文件 | 重点关注 |
|---|---|---|
| 15 | [app/agent/tool_cache.py](app/agent/tool_cache.py) | TTL 缓存设计，key 结构，为什么区分实时数据和目录数据 |
| 16 | [app/agent/tool_wrapper.py](app/agent/tool_wrapper.py) | 弹性层执行顺序：缓存→熔断→重试→超时→间接注入净化 |
| 17 | [app/agent/tools.py](app/agent/tools.py) | 6 个工具，重点看结构化 JSON 输出格式 `{status, data, summary}` |
| 18 | [app/agent/parallel_tools.py](app/agent/parallel_tools.py) | `ParallelToolRunner` + `make_parallel_query_tool` 元工具 |
| 19 | [app/agent/guardrails.py](app/agent/guardrails.py) | 双层注入防护：`InputGuardrails`（直接注入）+ `ToolOutputSanitizer`（间接注入） |
| 20 | [app/agent/checkpointer.py](app/agent/checkpointer.py) | Redis checkpointer 工厂，降级到 MemorySaver 的逻辑 |
| 21 | [app/agent/agent.py](app/agent/agent.py) | ReAct + `ReflectiveAgentRunner`，重点看自反思三维评分逻辑 |
| 22 | [app/agent/evaluation/langfuse_tracer.py](app/agent/evaluation/langfuse_tracer.py) | Langfuse Trace 和评分上报，了解 Agent 调用链路如何进入观测平台 |

---

## 第五轮：看懂 Multi-Agent 和 Plan-and-Execute（约 1 小时）

> 目标：理解两种不同的复杂任务处理模式及其适用场景的区别。

| # | 文件 | 重点关注 |
|---|---|---|
| 23 | [app/agent/multi_agent.py](app/agent/multi_agent.py) | **最复杂**，Supervisor + Send API 并行 fan-out + barrier + `SupervisorDecision` 结构化路由 |
| 24 | [app/agent/plan_execute.py](app/agent/plan_execute.py) | Plan-and-Execute 四节点图，`PlanExecuteState` reducer，Planner→Executor→Replanner→Synthesizer |

**看完问自己**：Multi-Agent 和 Plan-and-Execute 分别适合什么场景？这两者和 ReAct 有什么本质区别？

---

## 第六轮：看懂 RAG（约 1 小时）

> 目标：理解三阶段精排流水线，每阶段解决什么问题。

| # | 文件 | 重点关注 |
|---|---|---|
| 25 | [app/graph/embed_adapter.py](app/graph/embed_adapter.py) | Embedding 工厂，为什么要换掉 MockEmbedding |
| 26 | [app/services/query_rewriter.py](app/services/query_rewriter.py) | LLM 多查询改写，无 LLM 时降级逻辑 |
| 27 | [app/services/reranker.py](app/services/reranker.py) | Cross-Encoder 精排 + 上下文压缩，懒加载模型 |
| 28 | [app/services/knowledge_retrieval_service.py](app/services/knowledge_retrieval_service.py) | **重点看 `retrieve()` 方法的 9 个步骤**，特别是 ③-A（改写）、⑦-B（精排）、⑦-C（压缩）三个插入点 |
| 29 | [app/repositories/vector_store_factory.py](app/repositories/vector_store_factory.py) | Milvus 接口预留，理解 `StorageContext` 抽象层 |

---

## 第七轮：看懂 Service 层（约 45 分钟）

> 目标：理解外观服务如何把底层能力组合成对外接口。

| # | 文件 | 重点关注 |
|---|---|---|
| 30 | [app/services/agent_service.py](app/services/agent_service.py) | `async chat()`，工具缓存策略区分，Langfuse 评分注入 |
| 31 | [app/services/workflow_service.py](app/services/workflow_service.py) | `run_stream_async()`（asyncio.timeout 覆盖 async generator），幂等性缓存，`graph.invoke()` |
| 32 | [app/services/plan_execute_service.py](app/services/plan_execute_service.py) | Plan-and-Execute 外观，工具缓存策略与 AgentService 一致 |

---

## 第八轮：看懂工程能力（约 45 分钟）

> 目标：理解让系统在生产环境可靠运行的工程基础设施。

| # | 文件 | 重点关注 |
|---|---|---|
| 33 | [app/core/logging.py](app/core/logging.py) | `JSONFormatter` + `TraceIdMiddleware`，trace_id 怎么全链路透传 |
| 34 | [app/core/prompt_registry.py](app/core/prompt_registry.py) | 三层降级：Langfuse → YAML → 内置，Prompt 为什么不能硬编码 |
| 35 | [app/core/rate_limiter.py](app/core/rate_limiter.py) | 固定窗口限流器，生产升级路径（内存→Redis） |
| 36 | [app/agent/evaluation/langfuse_tracer.py](app/agent/evaluation/langfuse_tracer.py) | 链路追踪集成，`score_after_run` 自动评分 |
| 37 | [app/agent/evaluation/ragas_evaluator.py](app/agent/evaluation/ragas_evaluator.py) | Ragas 批量评测，`faithfulness` / `answer_relevancy` |

---

## 第九轮：对照单元测试（约 30 分钟）

> 目标：通过测试用例验证自己对每个模块的理解是否正确。  
> 读测试时重点问：这个 case 在测什么边界条件？

| # | 文件 | 对应验证的模块 |
|---|---|---|
| 38 | [tests/unit/test_workflow_nodes.py](tests/unit/test_workflow_nodes.py) | 每个节点的 state 增量格式，`Command` 短路路径 |
| 39 | [tests/unit/test_risk_evaluator.py](tests/unit/test_risk_evaluator.py) | CRITICAL / HIGH / MEDIUM / LOW 所有规则触发逻辑 |
| 40 | [tests/unit/test_tools_output.py](tests/unit/test_tools_output.py) | 6 个工具的 JSON schema：`status` / `data` / `summary` |
| 41 | [tests/unit/test_guardrails.py](tests/unit/test_guardrails.py) | 直接注入 + 间接注入双层防护，正常输入不被误拦截 |

---

## 学习路线总览

```
第一轮（系统边界）   README → config → main → router → .env.example
        ↓
第二轮（数据结构）   state → schemas/agent → schemas/workflow
        ↓
第三轮（Workflow）   router → trace → risk_evaluator → nodes → workflow → parallel_workflow
        ↓
第四轮（ReAct）      tool_cache → tool_wrapper → tools → parallel_tools
                    → guardrails → checkpointer → agent → observability
        ↓
第五轮（复杂模式）   multi_agent → plan_execute
        ↓
第六轮（RAG）        embed_adapter → query_rewriter → reranker
                    → knowledge_retrieval_service → vector_store_factory
        ↓
第七轮（Service）    agent_service → workflow_service → plan_execute_service
        ↓
第八轮（工程能力）   logging → prompt_registry → rate_limiter
                    → langfuse_tracer → ragas_evaluator
        ↓
第九轮（单元测试）   test_workflow_nodes → test_risk_evaluator
                    → test_tools_output → test_guardrails
```

---

## 面试高频问题对应文件

| 面试问题 | 对应文件 |
|---|---|
| ReAct 和 Plan-and-Execute 有什么区别？ | [agent.py](app/agent/agent.py) vs [plan_execute.py](app/agent/plan_execute.py) |
| LangGraph 的 Send API 怎么实现并行？ | [parallel_workflow.py](app/graph/parallel_workflow.py) |
| HITL 中断后状态怎么保存和恢复？ | [nodes.py](app/graph/nodes.py) + [workflow_service.py](app/services/workflow_service.py) |
| 工具缓存为什么区分实时和目录数据？ | [tool_cache.py](app/agent/tool_cache.py) + [agent_service.py](app/services/agent_service.py) |
| RAG 三阶段各解决什么问题？ | [query_rewriter.py](app/services/query_rewriter.py) + [reranker.py](app/services/reranker.py) |
| 间接 Prompt Injection 是什么？怎么防？ | [guardrails.py](app/agent/guardrails.py) |
| Prompt 为什么不能硬编码？ | [prompt_registry.py](app/core/prompt_registry.py) |
| `Command(goto=...)` 和普通 dict 返回有什么区别？ | [nodes.py](app/graph/nodes.py) 的 `order_analysis` 节点 |
| Multi-Agent 的 barrier 节点有什么作用？ | [multi_agent.py](app/agent/multi_agent.py) 的 `make_barrier_node` |
| 为什么换掉 MockEmbedding？ | [embed_adapter.py](app/graph/embed_adapter.py) |
