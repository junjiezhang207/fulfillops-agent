# Multiship Agent 测试报告

生成日期：2026-05-21  
测试目标：尽量用可重复、低外部依赖的自动化测试覆盖项目核心逻辑、API 合约、降级路径和基础性能烟测。

## 一、测试结论

当前默认测试集已通过：

```bash
uv run pytest -q
```

结果：

```text
199 passed, 58 skipped, 2 warnings
```

可选性能烟测已通过：

```bash
uv run pytest tests/performance/test_memory_performance_smoke.py --slow -q
```

结果：

```text
2 passed
```

`58 skipped` 主要是需要真实 LLM 或较慢环境的测试，默认跳过，避免普通开发测试变慢、变贵、不稳定。

## 二、本轮新增与修复

### 1. 新增限流测试

文件：

- `tests/unit/test_rate_limiter.py`

覆盖：

- 固定窗口内允许请求。
- 超出配额后拒绝请求。
- 时间窗口过期后自动恢复。
- 过期 key 清理。
- Agent / Workflow 超限时返回 429。

本轮还把限流真正挂到了 API 路由：

- `POST /api/v1/agent/chat`
- `POST /api/v1/agent/chat/stream`
- `POST /api/v1/agent/plan-execute`
- `POST /api/v1/workflow/run`
- `POST /api/v1/workflow/run/stream`
- `POST /api/v1/workflow/run/timeout`

### 2. 新增 QueryRewriter 测试

文件：

- `tests/unit/test_query_rewriter.py`

覆盖：

- 没有模型时回退原始 query。
- 同步改写结果解析。
- 异步改写结果解析。
- LLM 调用失败时安全降级。
- 列表编号、项目符号、重复 query 清理。

本轮测出了一个真实 bug：

- 缩进后的 `- 库存不足跨仓调拨` 没有去掉 `-`。

已修复：

- `app/services/query_rewriter.py` 中先 `line.strip()` 再清理编号/项目符号。

### 3. 新增上下文窗口测试

文件：

- `tests/unit/test_context_manager.py`

覆盖：

- 无 token counter 时不裁剪。
- 超过阈值时触发裁剪。
- 保留最近上下文。
- LangChain `trim_messages` 失败时保底保留尾部消息。
- `TrimResult.reduction_rate` 边界值。

### 4. 增强长期记忆测试

文件：

- `tests/unit/test_memory_store.py`

新增覆盖：

- MySQL + Milvus 工厂参数装配。
- MySQL + Milvus 后端缺少 MySQL URL 时明确报错。
- Milvus 向量命中被 filter 排除时，仍然并入 MySQL 文本兜底候选。
- COSINE / L2 相似度归一化。

### 5. 新增轻量 API 合约测试

文件：

- `tests/integration/test_lightweight_api_contract.py`

覆盖：

- `/api/v1/health` 基础可用性。
- `/api/v1/models` 不泄露 API Key 或 api_key_env。
- `/api/v1/models/active` embedding 模型合约。
- Agent chat 触发限流后不会继续执行模型服务。
- Agent chat 对 prompt injection 输入返回 422。
- Workflow 触发限流后不会继续执行业务服务。

注意：

这个测试使用轻量 FastAPI app 挂单个 router，不启动完整后端服务，避免真实模型、Redis、Milvus、MySQL 干扰。

### 6. 新增性能烟测

文件：

- `tests/performance/test_memory_performance_smoke.py`

覆盖：

- SQLite 长期记忆 600 条左右的小型商家数据集检索耗时不应明显退化。
- SQLite 长期记忆 300 条写入不应明显退化。

运行方式：

```bash
uv run pytest tests/performance/test_memory_performance_smoke.py --slow -q
```

说明：

这只是烟测，不是严格压测。真正 P95、并发、吞吐仍需要 Locust/k6 这类压测工具。

### 7. 修正旧测试异步调用问题

文件：

- `tests/test_agent_multi_turn.py`
- `tests/eval/test_agent_quality.py`

修正点：

- `AgentService.chat` 已经是 async，旧测试仍按同步函数调用，导致拿到 coroutine。
- 已改为 `async/await`。
- 真实 LLM 相关测试统一标记为 `slow`，默认跳过。

## 三、当前测试分层

### 默认测试

```bash
uv run pytest -q
```

适合每天跑、提交前跑、CI 跑。

覆盖：

- 核心业务逻辑。
- 工具输出。
- RAG 关键逻辑。
- 记忆系统。
- 模型网关配置解析。
- Guardrails。
- 限流器。
- 轻量 API 合约。

### 单元测试

```bash
uv run pytest tests/unit -q
```

结果：

```text
176 passed
```

特点：

- 快。
- 稳定。
- 不依赖真实 LLM / MySQL / Milvus。

### 慢速 / 真实模型测试

```bash
uv run pytest --slow
```

适合：

- 改 prompt 后。
- 切换模型后。
- 发版前做质量回归。

风险：

- 需要真实 API Key。
- 会消耗 token。
- 结果可能受模型波动影响。
- 运行时间较长。

### 性能烟测

```bash
uv run pytest tests/performance/test_memory_performance_smoke.py --slow -q
```

适合：

- 检查长期记忆是否出现明显性能退化。
- 作为正式压测前的轻量预警。

## 四、目前还能测出的 bug 类型

现在测试比较擅长发现：

- 配置解析错误。
- Pydantic schema 变更导致的 API 合约破坏。
- 降级逻辑失效。
- prompt injection 防护失效。
- 长期记忆 TTL / namespace / filter / 排序错误。
- RAG query 改写解析错误。
- 工具输出格式变化。
- 限流器失效。
- Agent async 接口被旧同步调用误用。
- API Key 泄露到前端接口响应。

## 五、目前不能靠这批测试充分证明的内容

以下内容不是“不能测”，而是不适合放进默认 pytest 里：

### 1. 真实 LLM 回答质量

需要运行：

```bash
uv run pytest tests/eval --slow
```

仍然无法保证 100% 稳定，因为模型输出本身有波动。

### 2. 商家多租户隔离

当前项目还没有完整 `merchant_id / tenant_id` 请求上下文，所以无法真正测试：

- A 商家的记忆不会影响 B 商家。
- A 商家的限流不会影响 B 商家。
- A 商家的知识库不会串到 B 商家。

后续需要先在 API schema、记忆 namespace、日志和限流 key 中引入 tenant 维度。

### 3. MySQL + Milvus 真实联调

当前单测只 mock 了 MySQL + Milvus 的关键分支。真实链路需要 Docker 或外部服务：

```bash
docker compose -f docker-compose.milvus.yml up -d
```

然后补充/运行专门的 integration 测试：

- MySQL 表是否自动创建。
- Milvus collection 是否自动创建。
- 写入 MySQL 后向量是否 upsert 到 Milvus。
- Milvus 不可用时是否降级 MySQL 文本检索。
- 大量记忆下检索是否仍可接受。

### 4. 并发和 P95 性能

pytest 只能做轻量烟测。真正要测：

- 10 / 50 / 100 并发商家请求。
- Agent chat P95。
- Workflow run P95。
- RAG retrieve P95。
- 模型供应商 429 时系统表现。
- 知识库重建时普通查询是否被拖慢。

建议使用：

- Locust
- k6
- hey / wrk
- Prometheus + Grafana

### 5. React 前端端到端行为

当前没有浏览器自动化测试。要测：

- 前端是否能连接后端。
- 表单提交是否正确。
- 流式输出是否渲染。
- 错误提示是否友好。

建议后续用 Playwright 做 E2E。

## 六、已知测试侧风险和建议

### 1. API 合约测试导入仍偏重

`tests/integration/test_lightweight_api_contract.py` 虽然没有启动完整 app，但导入 `agent` 路由仍会触发模块级依赖装配，运行时间偏长。

建议后续优化：

- 路由模块不要在 import 时创建 AgentService。
- 改成 FastAPI dependency 或懒加载 factory。
- 测试中可以更轻松 monkeypatch。

### 2. `--slow` marker 文案不够精确

当前 `--slow` 同时覆盖：

- 真实 LLM 测试。
- 性能烟测。

建议后续拆成：

- `slow`
- `llm`
- `performance`
- `external`

这样可以更细粒度地运行：

```bash
uv run pytest -m performance
uv run pytest -m llm --slow
uv run pytest -m external
```

### 3. 需要补真实服务 integration

下一批最有价值的是：

- `tests/integration/test_backend_smoke_live.py`
- `tests/integration/test_mysql_milvus_memory_live.py`
- `tests/integration/test_streaming_agent_contract.py`
- `tests/performance/test_api_latency_smoke.py`

这些建议不要默认跑，发版前或本地联调时跑。

## 七、推荐日常命令

开发中快速验证：

```bash
uv run pytest tests/unit -q
```

提交前验证：

```bash
uv run pytest -q
```

改模型、prompt、Agent 逻辑后：

```bash
uv run pytest tests/eval --slow
```

检查长期记忆轻量性能：

```bash
uv run pytest tests/performance/test_memory_performance_smoke.py --slow -q
```

## 八、总体评价

当前测试已经能覆盖项目大部分“可自动化且不依赖真实外部服务”的质量风险。它能较好发现代码回归、接口变形、降级失败、记忆检索错误和限流失效。

剩余不能充分自动化覆盖的部分，主要集中在真实 LLM 质量、真实 MySQL/Milvus/Redis 联调、大并发性能、前端端到端体验。这些需要单独的慢速测试、外部服务 integration 测试和压测工具，而不应该混进默认单元测试里。
