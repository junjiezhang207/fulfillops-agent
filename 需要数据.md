# 项目需要的数据清单

这份文档说明本项目为了功能测试、性能测试和面试演示，需要准备哪些数据。

结论先说清楚：**可以用 ChatGPT 生成合成数据**。你现在拿不到真实企业数据很正常，用合成数据做功能验证、压测、面试展示完全可以。需要注意的是，文档和 README 里要明确写成“synthetic data / 合成业务数据”，不要把它包装成真实生产数据。

---

## 1. 数据应该分成两类

这个项目不要把所有东西都塞进 RAG。真实电商系统里，数据通常分成两类。

### 1.1 RAG 知识数据

RAG 适合存“规则、政策、流程、说明”这类非结构化或半结构化知识。

适合放进 RAG 的数据：

| 数据类型 | 例子 | 项目用途 |
|---|---|---|
| 履约规则 | 缺货处理、拆单、合单、跨仓调拨 | 让 Agent/Workflow 解释为什么这么履约 |
| 平台规则 | 抖音、天猫、京东 SLA、超时赔付 | 判断不同平台订单的处理优先级 |
| 仓配规则 | 仓库覆盖区域、冷链限制、大件限制 | 决定从哪个仓发、是否需要拆单 |
| 售后政策 | 退货、换货、补发、退款规则 | 处理异常订单和售后问题 |
| 风险规则 | VIP、高金额、临近超时、黑名单地址 | 触发 HITL 或风险提示 |
| SKU 规则 | 可替代 SKU、不可替代 SKU、套装拆分 | 缺货时给替代建议 |
| 承运商规则 | 顺丰、京东物流、三方快递区域限制 | 选择物流方案 |
| 大促规则 | 618、双11、春节发货策略 | 模拟真实高峰业务 |

这些数据建议放在：

```text
app/data/knowledge/
```

当前项目默认读取的就是这个目录：

```text
KNOWLEDGE_DIR=app/data/knowledge
```

### 1.2 结构化业务数据

订单、库存、价格、物流轨迹这种数据不适合长期放 RAG，因为它们变化快，而且字段结构强。

应该走企业数据接口或数据库：

| 数据类型 | 是否放 RAG | 应该放哪里 |
|---|---:|---|
| 订单明细 | 否 | 企业订单接口 / `enterprise-data/orders/import` |
| 实时库存 | 否 | 企业库存接口 / `enterprise-data/inventory/import` |
| 商品价格 | 否 | 价格系统 / 数据库 |
| 物流轨迹 | 否 | 物流 API |
| 仓库实时产能 | 否 | WMS / 调度系统 |
| 用户历史订单 | 一般不放 | 订单系统 / 用户画像系统 |

当前项目已经有企业数据接入口，默认落盘到：

```text
storage/enterprise_data/
```

---

## 2. 为了测试项目，至少需要哪些数据

建议按三个阶段准备。

### 2.1 最小演示数据

适合本地调试和课堂学习。

| 数据 | 建议数量 |
|---|---:|
| RAG 文档 | 10 到 30 篇 |
| 订单 | 100 到 500 单 |
| SKU | 100 到 300 个 |
| 库存记录 | 300 到 1000 条 |
| 仓库 | 5 到 10 个 |
| 平台 | 3 到 5 个 |
| 区域 | 8 到 15 个 |

这个量级下，主要验证：

- RAG 能不能检索到规则
- 订单分析能不能跑通
- 库存分析能不能判断缺货
- Agent 能不能正确调用工具
- Workflow 能不能按路径执行

### 2.2 面试展示数据

适合让项目看起来更接近真实业务。

| 数据 | 建议数量 |
|---|---:|
| RAG 文档 | 50 到 200 篇 |
| RAG chunk | 1000 到 10000 个 |
| 订单 | 5000 到 50000 单 |
| SKU | 1000 到 10000 个 |
| 库存记录 | 10000 到 100000 条 |
| 仓库 | 20 到 100 个 |
| 承运商 | 5 到 20 个 |
| 售后规则 | 30 到 100 条 |

这个量级下，可以测试：

- RAG 索引构建时间
- 检索延迟
- Workflow 并发性能
- Agent 工具调用稳定性
- 企业数据导入速度
- 缓存命中率

### 2.3 压测数据

适合做性能测试，不一定要全部手工看懂。

| 数据 | 建议数量 |
|---|---:|
| RAG 文档 | 500 到 3000 篇 |
| RAG chunk | 50000 到 500000 个 |
| 订单 | 100000 到 1000000 单 |
| SKU | 50000 到 300000 个 |
| 库存记录 | 500000 到 3000000 条 |
| 仓库 | 100 到 500 个 |

这个阶段要注意：如果继续用本地 JSON 文件做企业数据存储，性能会很快到瓶颈。压测到这个量级时，更合理的是换成 MySQL/PostgreSQL/Redis/Milvus 这类真实存储。

---

## 3. ChatGPT 生成数据可以吗

可以，而且很适合你现在这个阶段。

### 适合用 ChatGPT 生成的数据

- 履约规则文档
- 平台 SLA 文档
- 售后政策文档
- 仓库覆盖区域说明
- SKU 替代规则说明
- 风险处置 SOP
- 合成订单 JSON
- 合成库存 JSON
- 压测用订单 ID、SKU ID、仓库 ID

### 不适合直接相信的部分

ChatGPT 生成的数据有几个问题：

- 规则可能不符合真实平台政策
- 数字分布可能不真实
- 订单和库存之间可能不一致
- SKU 替代关系可能缺少业务约束
- 生成大量 JSON 时可能出现格式错误

所以建议做两层处理：

1. 用 ChatGPT 生成模板和样例。
2. 再用脚本批量生成大规模数据，保证字段合法、ID 可控、订单和库存能对上。

---

## 4. RAG 文档建议怎么生成

RAG 文档最好不是纯随机文本，而是按业务主题生成，每篇文档有明确分类。

推荐目录：

```text
app/data/knowledge/
  stockout_rules.md
  priority_orders.md
  regional_fulfillment.md
  split_merge_orders.md
  after_sales_rules.md
  platform_sla_rules.md
  carrier_rules.md
  warehouse_capacity_rules.md
  sku_substitution_rules.md
  promotion_fulfillment_rules.md
```

每篇文档建议包含：

- 规则名称
- 适用平台
- 适用区域
- 适用订单类型
- 触发条件
- 处理动作
- 例外情况
- 人工审批条件
- 示例订单场景

### ChatGPT 生成 RAG 文档的提示词

可以直接用下面这个提示词：

```text
你是电商供应链履约专家。请生成一篇用于 RAG 知识库的业务规则文档。

要求：
1. 主题：缺货订单处理规则
2. 行业：全域电商，多平台订单，包括抖音、天猫、京东、自营商城
3. 内容包括：适用场景、触发条件、处理优先级、跨仓调拨、拆单、替代 SKU、人工审批、异常兜底
4. 写成 Markdown
5. 语言自然，像真实公司内部 SOP
6. 不要写成教程，不要解释 RAG
7. 至少包含 10 条规则和 5 个示例场景
```

---

## 5. 企业订单数据格式

企业订单通过接口导入：

```text
POST /api/v1/enterprise-data/orders/import
```

JSON 结构示例：

```json
{
  "source_id": "erp-main",
  "orders": [
    {
      "order_id": "SO-ENT-001",
      "platform": "ERP",
      "order_time": "2026-05-11T09:30:00",
      "order_status": "待履约",
      "region": "华东-上海",
      "priority": "高",
      "items": [
        {
          "sku_id": "SKU-ENT-001",
          "product_name": "企业导入商品",
          "quantity": 3,
          "unit_price": 99.0
        }
      ]
    }
  ]
}
```

建议生成的订单字段分布：

| 字段 | 建议值 |
|---|---|
| platform | 抖音商城、天猫旗舰店、京东自营、拼多多、自营商城、ERP |
| order_status | 待履约、已支付待审核、部分缺货、待人工审批 |
| region | 华东-上海、华南-广州、华北-北京、西南-成都、华中-武汉 |
| priority | 高、中、低 |
| items | 每单 1 到 5 行 |
| quantity | 每行 1 到 20 件 |

---

## 6. 企业库存数据格式

企业库存通过接口导入：

```text
POST /api/v1/enterprise-data/inventory/import
```

JSON 结构示例：

```json
{
  "source_id": "erp-main",
  "records": [
    {
      "warehouse_id": "WH-ENT-001",
      "warehouse_name": "企业上海仓",
      "region": "华东-上海",
      "sku_id": "SKU-ENT-001",
      "available_stock": 8,
      "locked_stock": 1,
      "updated_at": "2026-05-11T10:00:00"
    }
  ]
}
```

建议生成的库存字段分布：

| 字段 | 建议值 |
|---|---|
| warehouse_id | WH-SH-001、WH-GZ-002、WH-BJ-003 |
| warehouse_name | 上海一仓、广州中心仓、北京前置仓 |
| region | 和订单区域保持同一套区域体系 |
| sku_id | 和订单里的 SKU 能对上 |
| available_stock | 0 到 5000 |
| locked_stock | 0 到 1000 |

为了测试缺货场景，建议故意让 20% 到 35% 的订单出现库存不足。

---

## 7. 性能测试应该看哪些指标

### RAG 性能

关注：

- 索引构建耗时
- 单次检索耗时
- 命中文档数量
- top-k 相关性
- Query Rewrite 耗时
- Rerank 耗时
- 缓存命中率

可用接口：

```text
POST /api/v1/knowledge/retrieve
GET  /api/v1/metrics
```

### Workflow 性能

关注：

- 单次履约分析耗时
- 并发请求成功率
- 触发 HITL 比例
- 缺货路径比例
- RAG 节点耗时

可用接口：

```text
POST /api/v1/workflow/run
POST /api/v1/workflow/run/stream
GET  /api/v1/metrics
```

### Agent 性能

关注：

- 工具调用次数
- LLM 首 token 延迟
- 总响应时间
- 反思重试次数
- 工具缓存命中率
- Langfuse trace 是否完整

可用接口：

```text
POST /api/v1/agent/chat
GET  /api/v1/agent/cache/stats
GET  /api/v1/metrics
```

---

## 8. 建议的数据生成顺序

不要一上来生成百万数据。建议按这个顺序来：

1. 先生成 20 篇 RAG 规则文档。
2. 生成 200 个 SKU。
3. 生成 10 个仓库。
4. 生成 500 单订单。
5. 生成 1000 条库存记录。
6. 导入企业订单和库存。
7. 用 React 前端验证订单、库存分析和人工审查流程。
8. 重建 RAG 索引。
9. 跑 Workflow 和 Agent。
10. 再逐步扩大到 5000 单、50000 单。

---

## 9. 面试时怎么解释这批数据

可以这样说：

> 由于没有真实企业数据，我使用合成电商履约数据进行功能验证和性能测试。数据被分成两类：订单、库存这类强结构化实时数据走企业数据接入层；履约规则、平台 SLA、售后政策、仓配策略这类非结构化知识进入 RAG。这样设计是为了避免把 RAG 当数据库用，同时让 Agent 和 Workflow 能结合实时业务数据与规则知识做决策。

这段话很重要，能体现你理解真实业务系统的数据边界。

---

## 10. 最推荐先准备的数据

如果时间有限，优先准备这些：

| 优先级 | 数据 |
|---:|---|
| P0 | 订单数据 500 到 5000 单 |
| P0 | 库存数据 1000 到 10000 条 |
| P0 | 缺货规则、拆单规则、跨仓调拨规则 |
| P1 | 平台 SLA 规则 |
| P1 | 售后政策 |
| P1 | SKU 替代规则 |
| P2 | 大促规则 |
| P2 | 承运商规则 |
| P2 | VIP 客户协议 |

先把 P0 做好，项目就已经能完整演示“企业数据 + RAG + Agent + Workflow”的闭环。
