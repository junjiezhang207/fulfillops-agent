"""旧 LangGraph 路径兼容包。

新的企业级目录：
- ``app.workflows.fulfillment``：履约 Workflow 的状态、节点、路由、风险判断、Trace 和图编排。
- ``app.infrastructure.llm``：ChatModel 与 Embedding 的基础设施适配器。

这个 ``app.graph`` 包只保留旧导入路径兼容。新代码请优先使用上面的新路径。
"""
