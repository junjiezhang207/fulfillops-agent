# 项目修改说明

这个项目不是简单堆功能，我主要做过几轮工程化改造，让它更像真实企业里的供应链 Agent 系统。

## 1. Agent 编排改造

- 将 Agent 主流程更多交给 LangChain / LangGraph 的 `create_agent`、checkpointer 和 callback 体系处理。
- 自己保留业务适配层，负责工具装配、上下文裁剪、长期记忆、结构化输出和接口返回。
- 好处是减少手写 ReAct 循环，代码更贴近主流 Agent 工程实践。

## 2. RAG 管线拆分

- 将原来集中在检索服务里的逻辑拆成：
  - query 意图识别与扩展
  - 检索、重排、压缩
  - 答案摘要与建议动作生成
- 这样 RAG 不再是一个超大的类，后续调优检索、摘要、意图识别时更容易定位。

## 3. 工具层工程化

- 工具调用统一包装，增加超时、重试、缓存和错误降级。
- 对实时数据工具和目录类工具做了缓存策略区分，例如库存不缓存，知识库/替代品可以短缓存。
- 这样 Agent 调工具失败时不会直接拖垮整轮对话。

## 4. 企业数据上传体验优化

- 将企业数据上传改成更接近真实业务的方式：订单、库存等结构化 CSV / JSON 可以直接上传。
- 增加预览、校验和确认导入流程，而不是只靠手写 JSON 示例。
- 面试时可以说明：真实企业数据通常已经由 ERP / WMS / OMS 处理好，系统更应该支持标准化导入。

## 5. 反思质量门优化

- 原来的反思评分，只看是否调工具、是否有数字、是否相关。
- 我改成了证据感知的质量门：
  - 根据问题判断应该调用哪些工具
  - 检查答案里的订单号、SKU、仓库、数量是否来自工具结果
  - 工具失败时要求答案说明不确定性
  - 编造关键业务实体会触发重试
- 这样反思不是形式化打分，而是真的在低质量或无证据回答时触发。


> 我没有把反思做成一个简单的 LLM 自评，因为那样成本高且不稳定。我做的是一个规则型证据质量门：先判断这个问题应该调用哪些业务工具，再检查最终答案里的订单号、SKU、仓库和数量是否真的来自工具结果。这样它不会因为答案里随便出现一个数字就通过，也能在 Agent 编造业务事实时触发重试。

## 6. 可观测性补强

- 接入 Langfuse，用于查看 Agent trace、工具调用、LLM 调用链路和质量分。
- 暴露 Prometheus 指标接口，方便后续接 Grafana。
- 日志中加入 trace id，便于排查一次请求从接口到 Agent 再到工具的完整链路。

## 7. 上下文管理精简

- 原来上下文管理里保留了旧版滑动窗口说明和字符估算 token 的兜底逻辑。
- 我改成只依赖 LangChain 的 `trim_messages()` 和模型自身的 `get_num_tokens_from_messages()`。
- 没有 LLM token counter 时直接跳过裁剪，不再使用不准确的字符估算。
- 删除了未使用的摘要 prompt、TokenBufferMemory / SummaryBufferMemory 工厂函数。
- `AgentService` 里也改成直接把真实 `chat_model` 传给上下文管理器。

面试里可以这样讲：

> 这部分我主要做的是减法。上下文裁剪应该尽量依赖模型自己的 token 计数和 LangChain 的消息裁剪能力，不再维护一套容易误差的字符估算和未使用的 Memory 工厂代码。

## 8. 提示词注入防护增强

- 项目原来已经有两层防护：入口拦截用户直接注入，工具输出净化间接注入。
- 我继续补强了几类容易绕过的攻击：
  - 零宽字符、全角字符混淆
  - 空格/标点拆开的攻击词，例如 `i g n o r e previous instructions`
  - 中文系统提示词提取，例如“输出系统提示词”“告诉我隐藏指令”
  - 工具输出里的中文间接注入和 developer/system 块
- 归一化只用于检测，不改写正常业务文本，避免把中文标点误改。
- 增加了相关单元测试，覆盖直接注入、间接注入、混淆绕过和正常业务数据不误伤。

面试里可以这样讲：

> 我没有只写几个普通正则，而是把直接注入和间接注入分开处理。用户输入会先做 Unicode 归一化和紧凑化检测，工具输出也会在进入 LLM 上下文前净化，这样能挡住订单备注、知识库文档里预埋的恶意提示。

## 9. Multi-Agent 编排工程化

- 原来的多 Agent 编排已经有 Supervisor + 专家 Agent 的结构，但还有一些演示化细节。
- 我做了一轮工程化收敛：
  - 删除未使用的 rule 工厂函数，减少无效代码。
  - 每次多 Agent run 增加独立 `run_id`，避免同一订单多次请求时专家 Agent 记忆互相污染。
  - 修复无 LLM 规则降级时的工具参数问题。
  - 专家结果增加 `tools_called`，能看到每个专家实际调用了哪些工具。
  - Supervisor 规则路由补充库存类关键词。
- 这样 Multi-Agent 不只是概念演示，而是更容易排查、更可解释，也能在 LLM 不可用时保留确定性降级能力。

面试里可以这样讲：

> Multi-Agent 这块我没有只停留在“多个 Agent 一起回答”的概念上，而是补了运行隔离、规则降级、工具调用可观测和参数适配。这样即使没有 LLM Supervisor，系统也能按确定性规则跑出可解释的专家结果。

## 10. 校验与测试

- 使用 Pydantic 加强订单、库存、企业数据等入参校验。
- 为 RAG、工具层、反思质量门等关键逻辑补充单元测试。
- 最近一次单元测试结果：`122 passed`。

## 11. Langfuse 评分逻辑调整

- 原来的 Langfuse 评分里，如果没有反思分，只要 Agent 调用了工具，就会给 `overall_quality=0.7`。
- 这个逻辑不够严谨，因为“调用了工具”不等于“答案质量好”，工具可能调错、结果可能失败，答案也可能引用错。
- 我把评分改成了更可解释的质量信号：
  - `tool_grounding`：是否调用了工具。
  - `answer_completeness`：是否生成了非空答案。
  - `overall_quality`：只有启用反思质量门并拿到 `reflection_score` 时才写入。
  - `reflection_passed`：记录反思质量门是否通过。
- 这样 Langfuse 里不会把启发式估算伪装成真实质量分，而是区分“客观观测信号”和“质量门评分”。
- 增加了单元测试，防止以后又把 `调工具=高质量` 的逻辑写回来。

面试里可以这样讲：

> 我把 Langfuse 评分从粗糙的启发式分数改成了可解释信号。是否调用工具、是否生成答案属于客观观测；综合质量分只来自反思质量门。这样 trace 里的分数更可信，也方便后续接 Langfuse Eval 做真正的离线评测。

## 12. Agent vs Workflow 对比去掉伪质量打分

- 原来的对比框架会根据答案是否命中关键词给 Agent / Workflow 打 `quality_score`。
- 这个分数不够可靠，因为关键词命中不代表答案真的正确，也不能说明业务决策质量。
- 我把这部分改成只保留客观执行指标：
  - 是否成功执行
  - 执行耗时
  - 工具 / 步骤调用数量
  - 最终回复长度
  - 错误信息
- 推荐逻辑也不再依赖质量分，而是基于稳定性、耗时和路径复杂度。
- 历史对比报告也更新为说明文档，明确答案质量评估应交给 Ragas、DeepEval 或 Langfuse Eval。

面试里可以这样讲：

> Agent 和 Workflow 的对比是有价值的，但不能用关键词命中率冒充答案质量。我把对比框架收敛成执行成本和稳定性分析；真正的质量评估走专门的评测链路，比如 Ragas、DeepEval 或 Langfuse Eval。

## 13. 模型网关企业级升级

- 原来模型配置更偏本地 Demo，主要依赖 `.env` 里的单一模型配置。
- 我改成了后端 `ModelGateway` 统一管理模型清单、用途和密钥来源：
  - 模型清单放在 `config/model_gateway.yaml`，可以配置 DeepSeek、通义、Kimi、Claude 等不同模型。
  - 增加 `model_type`，把模型分成 `chat`、`embedding`、`reranker`，不再只管理对话模型。
  - 增加 `default_models`，可以按任务配置默认模型，而不是所有场景都走同一个大模型。
  - 增加 `tier` 和 `deployment`，区分强模型、小模型、云模型和本地开源模型。
  - Embedding 支持本地 HuggingFace BGE 和云端 OpenAI-compatible Embedding；Reranker 支持本地 Cross-Encoder 和云端 Jina Rerank API。
  - 前端只通过接口获取可选模型和当前默认模型，不接触任何 API Key。
  - Agent 和 Plan-and-Execute 请求可以传 `model_id`，但实际模型构造和 Key 读取仍在后端完成。
  - 后端根据 `use_case` 选择模型，例如 Agent、Workflow、RAG 改写、Judge 可以走不同模型。
  - RAG query 改写、RAG 片段压缩、结构化抽取、Workflow 最终话术、Agent 主对话、Multi-Agent Supervisor、评测 Judge 都已经拆成不同 `use_case`。
  - 本地开源 Chat 模型可通过 OpenAI-compatible 接口接入；本地 Embedding / Reranker 直接走 HuggingFace / sentence-transformers，并支持 `requires_api_key: false`。
  - 旧版 `LLM_PROVIDER / LLM_MODEL / LLM_BASE_URL` 仍保留为本地 Demo fallback，避免一次性破坏旧启动方式。
- 新增 `/api/v1/models` 和 `/api/v1/models/active`，方便前端展示模型选项和当前模型状态。
- React 前端可通过 `/api/v1/models` 和 `/api/v1/models/active` 展示模型供应商、模型 ID 和 Key 配置状态；发送 Agent 对话时可把选中的 `model_id` 传给后端。
- 增加了模型网关单元测试，确认模型元数据不会泄露密钥，并且能按用途选择默认模型。

面试里可以这样讲：

> 企业项目里不应该所有任务都用同一个大模型，也不应该让前端直接持有各家大模型 Key。所以我把多模型接入收口成后端模型网关：复杂决策和评测走强 Chat 模型，RAG 改写、片段压缩、结构化抽取走小 Chat 模型，向量化走 Embedding 模型，精排走 Reranker 模型。前端只选择模型 ID，后端负责模型路由、密钥读取和模型实例构造。

## 14. MCP 标准工具出口增强

- 原来的 MCP 更偏演示模块，业务 Server 自己 new 内存仓库，和前端上传的企业数据不是同一套来源。
- 我把业务 MCP Server 改成复用统一 `service_registry`，让 MCP、FastAPI、Agent、Workflow 使用同一套订单、库存、知识库服务。
- 文件系统 MCP 不再写死 `knowledge_base`，改成读取 `Settings.knowledge_dir`，和主 RAG 知识库目录保持一致。
- 文件读取安全边界加强：
  - 使用 `Path.relative_to()` 防路径穿越。
  - 只允许读取 Markdown 知识库文件。
  - 保留文件长度截断，避免一次返回过大内容。
- MCP Client 文档和实现对齐，补充同步脚本入口；同步入口也能自动 await MCP 工具的 `ainvoke()`。
- MCP Server 启动方式升级为双模式：
  - `stdio`：适合 Cursor、Claude Desktop、本地调试。
  - `streamable-http`：适合企业内网多客户端共享，后续可接网关鉴权、限流、审计。

面试里可以这样讲：

> MCP 在这个项目里不是替代内部 LangChain tools，而是对外标准工具出口。内部 Agent 仍然用本地 tools 保持低延迟和可控性；外部 AI 客户端通过 MCP 调用同一套 service registry。这样既避免工具重复冲突，也让企业数据、知识库和履约能力可以标准化开放出去。

## 15. 长短期记忆分层优化

- 短期记忆收敛成统一入口：`app.agents.checkpointer` 不再重复实现 Redis 连接逻辑，而是复用 `app.memory.short_term`。
- 短期记忆定位更清楚：只负责会话内多轮上下文，Redis 可用时持久化，不可用时降级到 MemorySaver。
- 长期记忆不再每轮都无脑写入，只沉淀更有复用价值的信息：
  - 用户偏好，例如是否接受替代 SKU、是否优先时效。
  - 订单决策，例如缺货、跨仓、延期、人工复核等处理结论。
  - 高价值业务摘要，例如风险、履约方案、售后、调拨等。
- 用户偏好改成稳定 key 写入，重复表达同一个偏好会更新同一条记忆，避免越写越多。
- 长期记忆搜索改为必须有文本相关性才召回，避免只因为 importance 高就把无关记忆塞进 prompt。
- 搜索命中的长期记忆会增加访问热度，后续排序能体现“经常被用到的记忆更重要”。
- 增加长期记忆测试，覆盖无关高权重记忆不应被召回。

面试里可以这样讲：

> 我把记忆系统分成了更明确的两层：短期记忆交给 LangGraph checkpointer 保存完整会话状态；长期记忆只保存可复用的业务资产，比如客户偏好、订单决策和风险结论。同时我加了写入门槛、偏好去重和相关性召回，避免长期记忆变成噪声仓库。

## 16. 长期记忆升级为 PostgreSQL + pgvector

- 新增 `PostgresPgvectorLongTermMemoryStore`，仍然实现 LangGraph `BaseStore`，不影响 Agent / Workflow 调用方式。
- 默认仍保留 SQLite，保证 Windows 本地开发和面试演示可以直接跑。
- 生产模式通过 `.env` 切换：
  - `LONG_TERM_MEMORY_BACKEND=postgres`
  - `LONG_TERM_MEMORY_POSTGRES_URL=postgresql://...`
  - `LONG_TERM_MEMORY_VECTOR_DIMENSION=512`
- PostgreSQL 负责保存结构化元数据：namespace、memory_type、importance、TTL、access_count、created_at、updated_at。
- pgvector 负责保存长期记忆文本 embedding，用于语义召回。
- 检索排序融合语义相似度、importance 和 access_count，比纯关键词更接近真实企业长期记忆方案。
- 没有配置 PostgreSQL 或 embedding 时不会破坏本地流程，仍可退回 SQLite 或文本检索。

面试里可以这样讲：

> 我把长期记忆从本地 SQLite 演示版升级成了可切换的 PostgreSQL + pgvector 生产版。SQLite 保证本地开箱即用；生产环境用 PostgreSQL 保存结构化记忆元数据，用 pgvector 做语义召回，同时保留 LangGraph BaseStore 接口，业务代码不需要感知底层存储变化。

## 17. RAG 向量数据库切换为 Milvus

- RAG 向量存储默认从本地文件切换为 Milvus，`VECTOR_STORE_TYPE=milvus` 成为主路径。
- 新增 `docker-compose.milvus.yml`，Windows 上用 Docker Desktop 可以直接启动 Milvus Standalone。
- 补充 `llama-index-vector-stores-milvus` 依赖，`KnowledgeRetrievalService` 仍通过 LlamaIndex `StorageContext` 接入，不改业务检索流程。
- Milvus 写入开启 `upsert_mode`，并把 RAG chunk 的 `node_id` 固定为 `document_id::chunk-xxxx`，避免服务重启或重建索引时重复写入同一片段。
- 新增 Milvus 配置：
  - `MILVUS_URI`
  - `MILVUS_COLLECTION`
  - `MILVUS_DIM`
  - `MILVUS_UPSERT_MODE`
  - `MILVUS_OVERWRITE`
  - `VECTOR_STORE_FALLBACK_TO_LOCAL`
- 本地开发没启动 Milvus 时可回退本地向量存储；生产环境建议关闭 fallback，避免静默降级。

面试里可以这样讲：

> 我把 RAG 的向量库主路径从本地索引升级为 Milvus。LlamaIndex 仍负责文档摄取和检索编排，Milvus 负责向量持久化和相似度召回；同时通过稳定 chunk_id + upsert 避免重复入库。这样本地可以兜底，生产可以直接接独立向量数据库。

## 18. Services 目录职责收敛

- 把 `session_memory_service.py` 的定位改清楚：它不再被描述成项目正式短期记忆，而是 Hybrid 路由的轻量会话缓存。
- 正式短期记忆仍由 LangGraph checkpointer + Redis/MemorySaver 负责，避免项目里出现两套“短期记忆”说法。
- 删除早期演示型 `realtime_knowledge_service.py` 和 `hybrid_rag_service.py`，避免项目里出现两套 RAG 入口，主 RAG 链路统一指向 `KnowledgeRetrievalService`。
- 新增懒加载 Embedding 代理，长期记忆 PostgreSQL + pgvector 模式不会在 Agent/Workflow 初始化时立即加载本地 embedding 权重。
- `ModelGateway` 和 Agent/Hybrid 路由里的 `print()` 改成 `logging`，方便生产日志采集和排查。
- 验证 `app.main` 导入不再触发 `Loading weights`，减少 FastAPI 启动卡住的问题。

面试里可以这样讲：

> 我梳理了 services 目录里的新旧服务边界：正式链路收敛到 Agent、Workflow、KnowledgeRetrievalService、ModelGateway；早期 Hybrid RAG 演示代码已经删除，不再和主 RAG、主记忆体系混淆。同时把模型加载改成懒加载，把 print 改成 logging，启动和排障都更接近生产项目。

## 19. 业务规则引擎工程化

- 原来的 `business_rule_engine.py` 更像把字段直接改掉的规则集合，调用方只能看到应用了哪些规则，看不到为什么改、改前是什么、有没有规则互相覆盖。
- 我把规则执行改成返回完整 `RuleExecutionResult`：
  - 保留原始订单副本，不再原地修改传入订单。
  - 每条规则记录 `before / after / priority / conflict_note`。
  - 低优先级规则不能静默覆盖高优先级规则，只记录冲突并保留高优先级结果。
  - 风险标签统一通过规则入口追加，避免重复标签和不可审计的直接 append。
- `RuleImpactAnalysis` 不再写死原始优先级，而是基于真实原始订单生成影响报告。
- `AdvancedHybridService` 改为使用规则执行报告，并把 `rule_details`、`rule_conflicts`、`pricing_adjustment` 返回给前端/API。
- 动态定价不再只返回一个价格数字，而是返回基础金额、最终金额、倍率和影响因子，方便解释 VIP 折扣、急单加价、库存紧张、质量要求等因素。
- 增加单元测试，覆盖原订单不被修改、规则冲突可审计、低优先级规则不能覆盖高优先级规则、API 服务层能返回规则明细和定价说明。

面试里可以这样讲：

> 这块我把业务规则从“脚本式改字段”升级成了“可审计的规则执行层”。真实企业里 VIP、急单、预算、交期这些规则要比大模型回答更确定，所以我让规则先稳定地约束订单，再把结果交给履约方案生成。每条规则都有 before/after 和冲突说明，低优先级规则不能覆盖高优先级决策，这样出现争议时能解释清楚系统为什么这么处理。

## 20. 短期记忆到长期记忆的企业级沉淀策略

- 原来的长期记忆写入主要靠关键词、订单号和工具数量判断，能避免乱写，但还不够像企业级 Memory Governance。
- 我把写入流程改成“候选记忆生成 + 写入评分 + 稳定 key 去重 + 敏感信息脱敏”：
  - 短期记忆仍由 LangGraph checkpointer 保存完整对话过程。
  - 长期记忆只沉淀后续可复用的信息，比如用户偏好、客户级偏好、订单决策和高价值业务摘要。
  - 寒暄、短确认、低价值对话不会进入长期记忆。
  - 订单决策必须有工具调用证据，避免把模型随口说的结论长期保存。
  - 偏好会写入 session 维度；如果识别到客户编号，也会写入 customer 维度，方便跨会话复用。
  - 写入前会脱敏邮箱、手机号、身份证号，避免长期记忆跨会话复用时泄露个人信息。
  - 记忆 key 改为基于内容 hash 的稳定 key，重复偏好或重复结论会覆盖/去重，而不是无限新增。
- 增加单元测试，覆盖低价值对话不写入、工具证据订单决策写入、敏感信息脱敏、客户级偏好沉淀。

面试里可以这样讲：

> 我没有把短期记忆简单同步到长期记忆。短期记忆保存完整会话状态，长期记忆只保存经过策略判断的业务资产。写入前会判断这条信息有没有复用价值、是否有工具证据、属于 session/order/customer 哪个维度，并且先做敏感信息脱敏和稳定 key 去重。这样长期记忆不会变成聊天记录垃圾桶，更接近企业里可治理、可审计、可复用的记忆系统。

## 21. Hybrid 响应缓存语义收敛

- 原来的 `client_cache_service.py` 名义上叫客户端 Prompt Caching，但实际缓存的是最终回答，不是模型服务端 token 前缀缓存。
- 我把它收敛成 `ResponseCacheService`：
  - 明确定位为接口层响应缓存，而不是 Prompt Caching。
  - 默认短 TTL，按订单隔离，不做跨订单共享。
  - 缓存 key 支持 `cache_context`，后续可以接订单更新时间、库存版本、知识库版本和模型 ID，避免业务数据变化后复用旧答案。
  - 只缓存 completed 响应，不缓存中断、错误或空回答。
  - 缺货、替代、调拨、延期、审批、人工、退款、风险等高风险业务问题不缓存。
  - 统计从写死 `tokens_saved=1700` 改成命中次数、写入次数、跳过次数、估算节省延迟。
- `HybridService` 改为使用 `response_cache` 语义，同时保留旧 `client_cache` 参数兼容。
- `AdvancedHybridService` 删除未使用的缓存依赖，避免构造参数看起来有功能但实际没用。
- API `/hybrid/cache` 仍保留，但文案改成 Hybrid 响应缓存统计。
- 增加单元测试，覆盖命中、cache_context 防旧数据复用、高风险问题跳过、旧名称兼容。

面试里可以这样讲：

> 我把原来容易误导的“客户端 Prompt Caching”改成了更准确的响应缓存。它不冒充模型厂商的 token 级缓存，只负责同订单、同问题、同业务上下文下的短期结果复用。同时对高风险业务决策直接跳过缓存，并把统计改成真实的命中和延迟节省。这样既保留了重复请求优化，又不会为了省成本牺牲业务正确性。

## 面试可以这样总结

我做这个项目时，不只是实现一个能聊天的 Agent，而是重点补了企业级 Agent 需要的几个能力：可靠工具调用、可追踪的执行链路、可维护的 RAG 管线、后端模型网关、MCP 标准工具出口、真实业务数据接入、质量门和测试保障。整体目标是让它从 Demo 更接近一个可以解释、可以排查、可以持续迭代的业务 Agent 系统。
