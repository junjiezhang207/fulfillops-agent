# Multiship Agent 面试题库（按当前代码校准版）

> 这份文档只写能被当前代码支撑的内容。  
> 面试时不要把“可配置 / 可演进 / 可选接入”说成“默认已经生产运行”。  
> 主线记住一句话：**Workflow 保稳定，Agent 保灵活，RAG 保依据，模型网关和 Guardrails 保工程边界。**

---


## 复习方式

这份文档已经按重要程度从高到低重排。新的 Q1-Q130 就是建议复习顺序：先背 Q1-Q25，再掌握 Q26-Q55，然后看 Q56-Q85，最后扫读 Q86-Q130。

---

## 第一部分：必背高频题

这一部分按面试命中率最高的问题排序，建议脱稿讲清楚。

---

**Q1：这个项目解决什么问题？**

A：解决订单履约决策中的“该怎么发、风险在哪里、缺货怎么办、依据是什么”。它把订单、库存、规则知识和履约建议组合起来，给运营人员一个可解释的处理建议，而不是只做闲聊。

核心代码：`app/api/`、`app/application/`、`app/workflows/fulfillment/`、`app/agents/`、`frontend/`

---

**Q2：为什么同时有 Workflow、Agent、RAG？**

A：三者解决的问题不同：

- Workflow：适合固定、可审计、需要 HITL 的流程，比如订单分析、库存判断、风险分支。
- Agent：适合开放式问题，模型可以按意图选择工具。
- RAG：提供企业规则、SOP、售后政策等依据，减少模型凭空编。

企业场景不能所有事情都交给 Agent 自由发挥；关键链路要稳定，开放追问要灵活，规则依据要可追溯。

---

**Q3：如果面试官问“是不是套壳大模型”，怎么答？**

A：不是。模型只是其中一部分。项目有清晰的工程边界：

- 实时订单和库存走结构化服务，不放进 RAG。
- 企业规则走 RAG，不写死在 prompt。
- Agent 通过工具访问业务能力，不直接查数据库。
- Workflow 保证关键路径可测试、可审计。
- Guardrails、限流、工具超时、缓存、熔断和评测观测做兜底。

---

**Q4：为什么要做模型网关？**

A：企业项目不会只用一个模型。复杂 Agent 可以用强模型，结构化抽取、RAG 改写、压缩可以用小模型，Embedding 和 Reranker 又是另一类模型。模型网关把“业务用途”和“具体模型配置”解耦，业务代码只传 `use_case` 或 `model_id`。

核心代码：`app/infrastructure/llm/model_gateway.py`、`config/model_gateway.yaml`

---

**Q5：RAG 在项目里负责什么？**

A：RAG 负责企业规则、售后政策、仓配 SOP、缺货处理流程等非结构化知识。它不负责实时订单和库存，因为订单库存是结构化、高频变化数据，应该走业务服务或数据库。

核心代码：`app/rag/knowledge_retrieval_service.py`

---

**Q6：当前 RAG 流程是什么？**

A：

```text
用户问题
 -> RAGQueryPlanner：识别意图、补充订单/库存上下文、扩展查询
 -> LlamaIndex 检索：BM25 + 向量召回 + QueryFusion / RRF
 -> BusinessRulePostprocessor：按业务意图做轻量加权
 -> Reranker：qwen3-rerank / BGE / Jina 精排
 -> RagAnswerBuilder：整理结论、规则、建议动作和覆盖说明
```

核心代码：`rag_query_planner.py`、`knowledge_retrieval_service.py`、`reranker.py`、`rag_answer_builder.py`

---

**Q7：Milvus 维度不一致怎么排查？**

A：先看当前使用的 Embedding profile 维度，再看 `.env` 的 `MILVUS_DIM` 和 Milvus collection 维度。如果从 BGE 512 维切到 `text-embedding-v4` 1024 维，不能复用旧 collection，建议新建 collection 或重建索引。长期记忆的 `LONG_TERM_MEMORY_VECTOR_DIMENSION` 也要同步。

核心代码：`app/repositories/vector_store_factory.py`、`app/infrastructure/llm/embedding_adapter.py`

---

**Q8：Workflow 适合什么场景？**

A：适合路径稳定、业务责任重、需要审计的履约流程。比如先查订单，再查库存，再根据库存和风险走不同节点，必要时触发 HITL 人工审批。

核心代码：`app/workflows/fulfillment/`、`app/application/workflow/workflow_service.py`

---

**Q9：HITL 是怎么实现的？**

A：风险评估节点会基于 `RiskEvaluator` 判断是否需要人工审批。遇到高风险时用 LangGraph `interrupt()` 停住，状态由 checkpointer 保存。恢复时通过 `Command(resume=decision)` 把人工决定传回图继续执行。

核心代码：`app/workflows/fulfillment/risk_evaluator.py`、`app/application/workflow/workflow_service.py`

---

**Q10：Agent 适合什么场景？**

A：适合开放式履约追问，比如“综合分析这个订单为什么风险高、缺货怎么处理、有无替代方案”。这类问题可能需要动态调用订单、库存、知识库、仓库、替代 SKU 和履约方案工具。

核心代码：`app/agents/orchestration/react_agent.py`、`app/agents/runtime/agent_service.py`

---

**Q11：Agent 工具有哪些？**

A：当前 AgentService 组装基础工具和扩展工具：

- `analyze_order`
- `check_inventory`
- `retrieve_knowledge`
- `search_warehouse_inventory`
- `find_substitute_sku`
- `generate_fulfillment_plan`

这些工具来自业务服务，再统一经过 `tool_wrapper` 加超时、重试、缓存、熔断和输出净化。

核心代码：`app/agents/tools/factory.py`、`app/agents/tools/wrapper.py`

---

**Q12：为什么用 LangChain create_agent，而不是手写 ReAct 循环？**

A：ReAct 循环本身不是项目核心价值。手写容易在工具调用格式、多轮消息、callbacks、LangGraph 状态和 checkpointer 上踩坑。项目把标准 Agent 循环交给 LangChain，自己专注业务工具、记忆、安全和质量门。

核心代码：`app/agents/orchestration/react_agent.py`

---

**Q13：反思质量门怎么打分？**

A：当前不是 LLM Judge，而是规则型评分：

- 工具路由与工具成功：最高 `0.35`
- 问答相关性：最高 `0.25`
- 证据支撑与可执行性：最高 `0.40`

代码默认阈值是 `0.6`，配置默认是 `0.65`，生产入口通常走配置。硬失败包括：需要工具却没调、工具失败却给肯定结论、答案编造工具结果中没有的 SKU / 仓库 / 订单号等。

核心代码：`app/agents/orchestration/react_agent.py`、`tests/unit/test_agent_reflection.py`

---

**Q14：当前 Multi-Agent 还不够生产级的点是什么？**

A：要诚实说两个点：

1. 普通 Agent 工具经过 `tool_wrapper`，Multi-Agent 专家工具当前是直接 `make_*_tool`，后续应统一接入包装层。
2. `/agent/chat` 已接 `InputGuardrails`，但 Hybrid / Multi-Agent 路由的 question 也应该统一接入输入安全检查。

这不是否定 Multi-Agent，而是说明扩展链路还需要补齐工程保护。

---

**Q15：长期记忆现在支持哪些后端？**

A：当前默认是 SQLite 本地落盘。配置 `LONG_TERM_MEMORY_BACKEND=postgres` 可以走 PostgreSQL + pgvector；配置 `mysql_milvus` 可以走 MySQL + Milvus。面试时不要说“默认就是 pgvector”，准确说法是：**默认 SQLite，生产可切 PostgreSQL + pgvector 或 MySQL + Milvus。**

核心代码：`app/memory/long_term.py`

---

**Q16：Guardrails 做到了什么？**

A：做了两层 Prompt Injection 防护：

- `InputGuardrails`：用户输入进入 Agent 前拦截直接注入、系统 prompt 提取、ChatML / Llama / Alpaca 定界符、Jailbreak 关键词、危险命令。
- `ToolOutputSanitizer`：工具输出进入 LLM 前净化订单备注、知识库片段、ERP 字段里的间接注入。

还有 NFKC 归一化、零宽字符清理和压缩文本检测。

核心代码：`app/agents/tools/guardrails.py`、`tests/unit/test_guardrails.py`

---

**Q17：Guardrails 还需要继续写吗？**

A：需要，但重点不是无限堆正则，而是补接入面和闭环：

- Hybrid / Multi-Agent 路由也要统一做输入检查。
- Multi-Agent 工具也应走工具输出净化。
- 安全事件应该接入指标和审计日志。
- 高风险工具要做权限分级，不让 Agent 自动执行真实发货、退款等动作。

---

**Q18：tool_wrapper 这层做什么？**

A：它是工具调用的工程保护层：超时、重试、熔断、缓存、异常转可读错误、工具输出净化。实时数据工具不缓存，目录/规则类工具短 TTL 缓存。

核心代码：`app/agents/tools/wrapper.py`、`app/agents/tools/cache.py`

---

**Q19：怎么证明 Agent 回答质量？**

A：不能只靠一次演示。可以看：

- 是否调用了正确工具。
- 是否引用工具或 RAG 证据。
- 是否命中 Golden Dataset 的关键字段。
- 反思质量门是否通过。
- Langfuse trace 和 DeepEval / Ragas 离线评测结果。

核心代码：`app/agents/quality/evaluation/`、`tests/eval/`

---

**Q20：这个项目哪里还不够生产级？**

A：可以诚实说：

1. 企业数据还主要是本地导入和模拟仓储，生产要接真实 ERP/WMS/OMS。
2. 权限体系还不完整，需要 RBAC、租户隔离、操作审计。
3. Guardrails 需要覆盖 Hybrid / Multi-Agent 全入口，并接安全指标。
4. 知识库需要版本审批、灰度发布和回滚。
5. 模型网关可以继续接预算控制、fallback、熔断和灰度。

---

**Q21：为什么要保留 Workflow，不全部 Agent 化？**

A：企业关键流程要可控、可测、可审计。Workflow 是业务底盘，Agent 是智能增强。库存判断、风险审批、高价值订单处理不能完全依赖模型自由生成。

---

**Q22：这个项目你最核心的取舍是什么？**

A：最核心的取舍是没有让 Agent 全包。供应链履约里有些逻辑必须稳定，比如库存判断、风险审批、缺货分支，所以我保留 Workflow 作为业务底盘；Agent 只处理开放追问和综合分析；RAG 只放规则知识，不放实时订单库存。这个边界让系统更像企业应用，而不是聊天 Demo。

---

**Q23：这个项目里最能体现工程能力的是哪一块？**

A：我会讲 Agent 工具链路。它不是把工具函数直接塞给模型，而是有一整层工程保护：工具 schema、缓存策略、超时、重试、熔断、工具输出净化、trace 和质量门。这样即使模型不稳定，业务系统也有边界和兜底。

---

**Q24：如果问“你做这个项目最大的收获是什么”，怎么答？**

A：最大的收获是：大模型应用难点不是调用模型，而是系统边界。哪些交给规则，哪些交给 Agent，哪些放 RAG，哪些必须走结构化数据服务，模型失败、安全、成本、延迟和评测怎么兜底，这些才决定项目能不能从 Demo 走向真实业务。

---

**Q25：如果问“你怎么证明自己不是只调 API”，怎么答？**

A：我会从工程闭环讲：模型网关、工具包装、RAG 检索链、LangGraph 状态图、Guardrails、记忆、评测、可观测、知识库管理、数据 repository。这些都是大模型 API 之外的系统工程工作。

## 第二部分：重点深挖题

这一部分适合在第一轮项目介绍后应对追问，要求能结合代码讲。

---

**Q26：HybridService 是什么？**

A：`HybridService` 是一个总入口式路由服务。它用轻量意图分类把问题分为 simple / medium / complex / multi_domain：

- simple：走 Workflow。
- medium / complex：走单 ReAct Agent。
- multi_domain：优先走 Multi-Agent，失败时再降级。

核心代码：`app/application/routing/hybrid_service.py`、`app/application/routing/intent_classifier.py`

---

**Q27：当前模型配置怎么讲才准确？**

A：`config/model_gateway.yaml` 里当前默认配置是：

- `agent / workflow / plan_execute / supervisor / judge`：`deepseek-v4-pro`
- `workflow_finalize / structured_extract / rag_rewrite / rag_compress`：`deepseek-v4-flash`
- `embedding`：`aliyun-text-embedding-v4`，维度 1024
- `reranker`：`aliyun-qwen3-rerank`

但要补一句：`.env` 和 `Settings` 里仍保留了旧版 fallback 配置，比如本地 BGE 和 512 维默认值。真正运行时要保证模型网关、`MILVUS_DIM`、`LONG_TERM_MEMORY_VECTOR_DIMENSION` 三者一致。

---

**Q28：为什么 API Key 不能放前端？**

A：模型 Key 是服务端密钥。前端只应该看到模型 ID、展示名、是否可用、价格元数据，不能接触真实密钥。当前 `ModelProfile.public_dict()` 会返回安全字段，不会返回真实 API Key。

核心代码：`app/infrastructure/llm/model_gateway.py`

---

**Q29：模型不可用时系统会怎样？**

A：Agent 主链路需要真实 ChatModel。没有配置模型时，Agent / Plan-and-Execute 接口会返回 503 或服务不可用，但健康检查、基础订单/库存接口、部分 Workflow 规则链路仍能运行。这是为了让项目在没配 LLM 时也能启动和测试工程部分。

核心代码：`app/api/routes/agent.py`、`app/infrastructure/llm/model_gateway.py`

---

**Q30：为什么用 LlamaIndex 做 RAG、LangChain 做 Agent？**

A：LlamaIndex 在文档读取、切片、索引、BM25、向量检索和 QueryFusion 上更适合 RAG；LangChain / LangGraph 在工具调用、Agent 循环、状态图和 checkpointer 上更成熟。项目让框架各做擅长的部分。

---

**Q31：知识库管理现在做到什么程度？**

A：已有知识文档列表、上传、查看、删除、后台重建索引和状态查询。上传或删除文档后可以触发 `BackgroundTasks` 后台重建，避免接口长时间阻塞。文档 ID 会做安全化，防止路径穿越。

核心代码：`app/api/routes/knowledge_mgmt.py`

---

**Q32：RAG 如果 Milvus 没启动怎么办？**

A：开发环境可以通过 `VECTOR_STORE_FALLBACK_TO_LOCAL=true` 降级到本地索引，方便演示和测试。生产环境不建议静默降级，应该设为 false，让问题显式暴露并告警。

---

**Q33：Workflow 怎么跑？**

A：

```text
app/api/routes/workflow.py
  -> WorkflowService
  -> app/workflows/fulfillment/graph.py 搭图
  -> app/workflows/fulfillment/nodes.py 执行节点
  -> app/workflows/fulfillment/router.py 做条件分支
  -> app/workflows/fulfillment/state.py 定义状态
```

回答重点：`workflow.py` 搭图，`nodes.py` 做事，`router.py` 决定下一步。

---

**Q34：同步 Workflow 和流式 Workflow 有什么区别？**

A：同步 `run()` 为了兼容普通 HTTP 请求，会自动批准 HITL 检查点并返回最终结果。流式 / hybrid 路径更接近真实审批，遇到 interrupt 会把审批上下文返回给前端，等待人工 resume。

---

**Q35：Agent 流式输出怎么做？**

A：`/api/v1/agent/chat/stream` 调用 `AgentService.stream_chat()`，把 LangGraph / LangChain 的 stream 事件转成 NDJSON 行。事件包括 `status`、`token`、`tool_call`、`tool_result`、`done`、`error`。流式接口优先首字响应，所以不做前置反思。

---

**Q36：为什么反思不用 LLM Judge？**

A：在线链路需要便宜、稳定、可解释。当前质量门只兜底明显问题，不追求完美评价。LLM Judge 更适合离线评测或 Langfuse / DeepEval 场景，不适合每次在线请求都调用。

---

**Q37：Agent 为什么先自然语言回答，再结构化抽取？**

A：自然语言更适合用户阅读，也能保留工具分析过程；结构化字段更适合前端展示、审计和后续处理。项目先让 Agent 正常回答，再用 `with_structured_output(FulfillmentDecision)` 抽取风险、建议动作、引用来源等字段。

核心代码：`app/agents/runtime/agent_service.py`、`app/schemas/agent.py`

---

**Q38：Plan-and-Execute 和 ReAct 有什么区别？**

A：ReAct 是边看边调工具，适合中等复杂问题；Plan-and-Execute 是先规划全局步骤，再逐步执行，并由 Replanner 判断是否继续，适合步骤有依赖关系的问题，例如先查订单、再查库存、再找替代、最后生成履约方案。

核心代码：`app/agents/orchestration/plan_execute.py`、`app/agents/runtime/plan_execute_service.py`

---

**Q39：Multi-Agent Supervisor 解决什么问题？**

A：它用于跨领域复杂问题。当前拆成三个专家：库存专家、履约规划专家、风险专家。Supervisor 负责决定先并行调用哪些专家、是否补问，最后 Synthesizer 汇总答案。

核心代码：`app/agents/orchestration/multi_agent.py`、`app/agents/runtime/multi_agent_service.py`

---

**Q40：短期记忆为什么用 Redis？**

A：Redis checkpointer 可以让会话在服务重启或多实例部署时继续存在。Redis 不可用时会降级到 MemorySaver，开发环境可接受，但生产要用真实 Redis 或等价持久化 checkpointer。

核心代码：`app/agents/runtime/checkpointer.py`、`app/memory/short_term.py`

---

**Q41：为什么不是所有聊天都写长期记忆？**

A：长期记忆只存可复用、稳定、有价值的信息，比如用户偏好、人工确认过的处理结论、订单处理摘要。写入前会做候选打分、去重、长度限制和敏感信息脱敏，避免把手机号、邮箱、身份证等直接标识长期保存。

核心代码：`app/agents/runtime/agent_service.py`

---

**Q42：PII 脱敏在哪里做？**

A：不要说 `guardrails.py` 已经完整做 PII。当前轻量脱敏主要在长期记忆入库前，避免把邮箱、手机号、身份证等直接个人标识长期持久化。`guardrails.py` 主职责是 Prompt Injection。

核心代码：`app/agents/runtime/agent_service.py` 的 `_redact_sensitive_text()`

---

**Q43：为什么订单和库存不缓存，知识和替代 SKU 可以缓存？**

A：订单和库存是实时数据，缓存可能导致履约决策过期；知识规则和替代关系变化频率低，短时间缓存可以降低延迟和成本。

---

**Q44：Langfuse、日志、Prometheus 分别看什么？**

A：

- Langfuse：看 LLM trace、prompt、工具调用链、模型输入输出和质量分。
- 结构化日志：看服务运行、trace_id、异常排查。
- Prometheus 指标端点：子路由是 `/metrics`，被总路由挂载后默认访问 `/api/v1/metrics`；看请求数、耗时、工具缓存命中、工具调用、Workflow 状态等指标。

核心代码：`app/agents/quality/evaluation/langfuse_tracer.py`、`app/core/logging.py`、`app/api/routes/metrics.py`

---

**Q45：企业数据现在怎么接入？**

A：当前通过企业数据路由导入订单和库存记录，底层是文件/本地仓储形态，服务层通过 repository 读取。后续替换成 MySQL、PostgreSQL、ERP / WMS API 时，上层服务接口可以基本不变。

核心代码：`app/api/routes/enterprise_data.py`、`app/repositories/enterprise_data_repository.py`

---

**Q46：如果 Agent 没调用期望工具，怎么排查？**

A：

1. 看工具是否注册到 AgentService。
2. 看工具 description 是否清楚。
3. 看用户问题是否缺少订单号或 SKU。
4. 看 system prompt 是否明确工具使用边界。
5. 看 Guardrails 是否提前拦截。
6. 看 Langfuse trace / 本地 trace 是否出现 tool_call。

---

**Q47：如果 RAG 引用了错误规则，怎么排查？**

A：

1. 看 QueryPlanner 生成的查询。
2. 看召回文档 source、category、score。
3. 看 BM25 / 向量 / RRF 后排序。
4. 看 Reranker 前后排名。
5. 看文档本身是否过期或 metadata 错。
6. 看 AnswerBuilder 是否过度总结。

---

**Q48：AgentService 为什么要把工具分成 realtime 和 catalog？**

A：因为数据时效性不同。订单、库存、履约方案属于实时业务数据，缓存可能导致错误决策；知识库规则、替代 SKU 关系变化频率低，可以短 TTL 缓存降低延迟和成本。所以 `AgentService` 先按缓存策略拆工具，再统一交给 `wrap_all_tools()` 包装。

核心代码：`app/agents/runtime/agent_service.py` 的 `_split_by_cache_policy()`、`_wrap_tools()`

---

**Q49：ReflectiveAgentRunner 为什么放在 Agent 外面，而不是写进 prompt？**

A：放外面更可测试、可关闭、可对比。写进 prompt 只能期待模型自我检查，没法稳定知道为什么失败；外层质量门能返回结构化分数和 reason，也能决定是否重试。关闭反思时就是标准 ReAct Agent，不侵入主链路。

核心代码：`app/agents/orchestration/react_agent.py` 的 `ReflectiveAgentRunner`

---

**Q50：_extract_reply 为什么要截取当前用户消息之后的消息？**

A：因为同一个 `session_id` 会有多轮历史。如果不截取当前 turn，反思评分可能把上一轮工具调用也算进去，导致“这一轮没调工具却被误判为调过”。所以 `_extract_reply()` 会定位当前 HumanMessage，只分析它后面的 AIMessage 和 ToolMessage。

核心代码：`app/agents/orchestration/react_agent.py` 的 `_extract_reply()`

---

**Q51：ToolOutputSanitizer 为什么选择替换注入片段，而不是整个工具结果报错？**

A：工具结果里可能只有一小段备注被污染，其他库存数量、订单号、规则内容仍然有业务价值。替换注入片段可以保留干净业务数据，同时阻断恶意指令进入 LLM。只有混淆型注入很难定位具体片段时，才返回整体安全占位符。

核心代码：`app/agents/tools/guardrails.py`

---

**Q52：knowledge_mgmt 为什么重建索引用 BackgroundTasks？**

A：索引重建可能要读文件、切片、向量化、写 Milvus，本身是秒级到分钟级任务。直接在 HTTP 请求里同步等待会导致接口超时和前端卡住。现在上传/删除后可以后台重建，通过 `/status` 查询进度。

核心代码：`app/api/routes/knowledge_mgmt.py`

---

**Q53：Multi-Agent 为什么有 barrier 节点？**

A：并行专家会同时返回结果，Synthesizer 或 Supervisor 不能只看其中一个。`barrier` 相当于 fan-in，把一轮专家结果收齐后再交回 Supervisor 判断是否继续补问或进入最终汇总。

核心代码：`app/agents/orchestration/multi_agent.py`

---

**Q54：为什么长期记忆写入前要做候选打分和去重？**

A：不是每句话都有长期价值。如果把所有聊天都存进去，会污染记忆库，后续召回噪声变大。候选打分筛掉低价值内容，去重 key 避免同一订单结论重复写入，脱敏避免持久化直接个人标识。

核心代码：`app/agents/runtime/agent_service.py`

---

**Q55：如果讲一个你亲手解决的典型问题，可以讲什么？**

A：可以讲 Embedding 维度不一致。项目从本地 BGE 512 维切到阿里云 `text-embedding-v4` 1024 维后，旧 Milvus collection 或旧本地索引可能还是旧维度，检索时报维度不匹配。我的处理思路是把模型维度放进模型网关配置，同时强调 `MILVUS_DIM`、collection 和长期记忆维度必须一致，切换维度时新建 collection 或重建索引。

## 第三部分：加分成熟题

这一部分体现工程成熟度、排障能力和生产意识。

---

**Q56：项目分层怎么讲？**

A：

```text
React / TypeScript 前端
  -> FastAPI 路由层（限流、输入校验、响应包装）
  -> Service 层（WorkflowService / AgentService / HybridService / RAG）
  -> LangGraph / LangChain / LlamaIndex
  -> Repository / 企业数据 / Milvus / Redis / 长期记忆
  -> 模型网关 / Langfuse / Prometheus 指标
```

路由层不写业务决策，Service 层负责装配依赖，Graph / Agent 层负责执行推理和工具调用。

---

**Q57：DeepSeek thinking mode 为什么关闭？**

A：DeepSeek V4 配置里通过 `extra_body.thinking.type=disabled` 关闭思考模式，主要是为了避免多轮对话和流式输出中 reasoning content 回传不一致导致接口错误，同时让 Agent 输出更稳定。

---

**Q58：Workflow 为什么还需要 LLM？**

A：Workflow 的核心判断不是让 LLM 自由决定，而是节点和规则控制流程。LLM 主要用于最终话术整理或总结，把结构化状态转成自然语言报告。即使 LLM 失败，也不应该影响底层订单、库存和风险信号的记录。

---

**Q59：ContextWindowManager 做什么？**

A：它控制多轮对话上下文长度，使用模型自己的 token 计数能力和 LangChain `trim_messages()` 裁剪历史，避免 prompt 越来越长、成本升高或超过模型窗口。没有 token_counter 时选择不裁剪，避免误删消息结构。

核心代码：`app/agents/runtime/context_manager.py`

---

**Q60：为什么 Planner 要结构化输出？**

A：Planner 输出 `Plan(steps=list[str])`，程序才能稳定读取步骤并控制循环。如果只是自然语言计划，Executor 和 Replanner 都要猜文本含义，不利于测试和错误恢复。

---

**Q61：Multi-Agent 图结构怎么讲？**

A：

```text
START -> supervisor
       -> parallel / specialist agent
       -> barrier
       -> supervisor
       -> synthesizer -> END
```

第一轮通常并行调用多个专家，`barrier` 收齐结果后交回 Supervisor。如果信息足够就汇总，否则继续派发补充问题。

---

**Q62：为什么工具输出也要安全净化？**

A：间接注入更隐蔽。攻击内容可能藏在订单备注、知识库文档或外部系统字段里。Agent 调工具后，模型会把工具结果当上下文，所以必须在工具输出进入 LLM 前净化。

---

**Q63：并行工具有什么意义？**

A：有些查询彼此独立，比如订单、库存、知识库可以并发执行。项目里有 `ParallelToolRunner` 和 `parallel_query` 元工具，用 asyncio 并行调用多个工具，降低整体等待时间。

核心代码：`app/agents/orchestration/parallel_tools.py`

---

**Q64：DeepEval / Ragas 是不是每次请求都跑？**

A：不是。它们更适合离线评测或 CI 回归。在线请求主要靠规则型质量门、工具证据和 Langfuse 观测，不能每次都跑昂贵的 LLM-as-judge。

---

**Q65：Prometheus 指标现在完整吗？**

A：已有指标端点和一批指标定义，但面试时不要夸成完整生产监控闭环。更准确的说法是：项目预留了 Prometheus 指标体系，工具缓存命中率等已有同步，后续可以把更多请求耗时、token、成本和安全事件在实际调用点打点。

---

**Q66：知识库怎么更新？**

A：通过 `knowledge_mgmt` 路由上传/删除文档，再后台重建索引。当前有文档状态、fingerprint、后台重建状态查询，但严格的版本审批、灰度发布、回滚还属于后续演进。

---

**Q67：Agent 会不会直接影响真实发货？**

A：当前不会。Agent 输出的是分析和建议，不直接执行发货、退款、改地址等高风险动作。真正生产要把这些动作放到受权限控制的工具里，并要求人工审批、幂等、审计和补偿机制。

---

**Q68：如果线上出现维度错误，怎么处理？**

A：检查 Embedding profile 的 `dimension`、`.env` 的 `MILVUS_DIM`、长期记忆维度、Milvus collection 维度。维度不一致时不要硬复用旧 collection，应该新建 collection 或重建索引。

---

**Q69：tool_wrapper 为什么保留 BaseTool.invoke，而不是直接调用 tool.func？**

A：直接拆 `tool.func` 会绕开 LangChain 工具自己的 args_schema、回调、异步能力和未来扩展。当前用 `BaseTool.invoke()` / `ainvoke()`，外层只加超时、重试、缓存、熔断和输出净化，不破坏 LangChain 工具体系。

核心代码：`app/agents/tools/wrapper.py`

---

**Q70：为什么反思里要保存 ToolObservation，而不是只保存 tools_called？**

A：只知道“调过工具”不够。模型可能调了库存工具，但答案里编了一个工具结果里没有的 SKU 或数量。`ToolObservation` 保存工具返回内容，质量门可以比对答案里的订单号、SKU、仓库、数字是否有证据支撑。

---

**Q71：ContextWindowManager 没有 token_counter 时为什么不裁剪？**

A：没有可靠 token counter 时强行估算并裁剪，可能破坏消息结构，尤其是 tool call / tool result 成对关系。当前选择保守策略：没有模型计数器就不裁剪。这样可能多花 token，但不会误删上下文导致 Agent 格式错误。

核心代码：`app/agents/runtime/context_manager.py`

---

**Q72：PromptRegistry 的降级链有什么意义？**

A：Prompt 不能散落在代码里。当前优先从 Langfuse 取线上版本，失败时读本地 YAML，再失败用内置兜底。这样既能在线迭代 prompt，又不会因为 Langfuse 或配置文件异常导致服务直接不可用。

核心代码：`app/core/prompt_registry.py`

---

**Q73：SupervisorDecision 为什么要结构化输出？**

A：Supervisor 是路由器，必须稳定告诉程序下一步去哪个节点、有没有 follow_up、理由是什么。如果让它自由文本输出，程序还要解析自然语言，容易路由错误。Pydantic 结构化输出让 LangGraph 条件边更可靠。

---

**Q74：Plan-and-Execute 为什么要有 max_iterations？**

A：Planner / Replanner 都由模型参与，理论上可能不断生成新步骤导致循环。`max_iterations` 是防死循环保护，到达上限后强制进入 Synthesizer，总结已有结果并说明信息边界。

核心代码：`app/agents/orchestration/plan_execute.py`

---

**Q75：为什么 AgentService 要在调用前拼接“长期记忆参考”？**

A：模型不会自动知道长期记忆里有什么。Service 层先按 session / order namespace 检索相关记忆，再显式拼进当前问题，让 Agent 在本轮回答时能看到历史偏好或处理结论。这比把长期记忆混进短期消息历史更可控。

核心代码：`app/agents/runtime/agent_service.py` 的 `_message_with_memory()`

---

**Q76：为什么企业数据用 repository 包一层？**

A：API、Workflow、Agent 都不应该关心数据来自本地文件、内存、MySQL 还是 ERP API。Repository 把数据来源封装起来，后续替换真实数据库时，上层 service 基本不用改。

核心代码：`app/repositories/enterprise_data_repository.py`

---

**Q77：为什么要把“模型价格”放进 model_gateway.yaml？**

A：模型价格会随厂商、合同、缓存命中策略变化，写死在业务代码里很难维护。放进模型配置后，前端可以展示成本元数据，后续网关层也可以统一做预算控制和成本统计。

核心代码：`config/model_gateway.yaml`、`app/infrastructure/llm/model_gateway.py`

---

**Q78：如果面试官质疑“这是不是过度设计”，怎么答？**

A：我会承认有些能力是为了展示企业级 AI 应用边界，比如 Multi-Agent、MCP、长期记忆和评测。但主线并不复杂：Workflow 处理确定流程，Agent 处理开放问题，RAG 提供规则依据。其他模块都是围绕这条主线做安全、记忆、观测和扩展，不是为了炫技。

---

**Q79：如果问“你怎么验证改动没有破坏核心链路”，怎么答？**

A：我会先跑无 LLM 单测，比如 guardrails、reflection、context manager、tools output、workflow nodes；再跑轻量 API contract 测试，确认路由拦截、限流和响应格式；最后再用少量 eval case 看 Agent / RAG 质量。这样工程逻辑和模型效果分层验证。

---

**Q80：如果面试官问“你这个系统一次请求完整链路是什么”，怎么答？**

A：以 `/agent/chat` 为例：FastAPI 路由先做限流和输入 Guardrails，然后根据 `model_id` 获取或创建 `AgentService`；Service 层检索长期记忆、裁剪上下文、运行 LangChain ReAct Agent；Agent 通过工具访问订单、库存、知识库等服务；工具结果经过 `tool_wrapper` 做超时、缓存、净化；最后 Service 做结构化抽取、写长期记忆、上报 Langfuse，并返回统一响应。

---

**Q81：如果问“你的 Agent 怎么避免瞎编订单数据”，怎么答？**

A：三层约束：第一，系统提示要求业务事实来自工具；第二，工具层返回结构化 JSON 和摘要；第三，反思质量门会检查答案中的订单号、SKU、仓库、数字是否能在工具结果或用户问题中找到。如果出现工具结果未支撑的关键实体，会判为硬失败并触发重试。

---

**Q82：如果问“为什么 Response Cache 不能缓存所有回答”，怎么答？**

A：履约决策有风险和时效性。低风险、completed 的结果可以短时间缓存，但 HITL、中断、错误、高风险决策不应该缓存。否则可能把过期库存、旧规则或未审批结论复用到新的业务场景。

核心代码：`app/application/cache/client_cache_service.py`、`app/application/routing/hybrid_service.py`

---

**Q83：如果问“怎么做成本控制”，怎么答？**

A：成本控制不是只换便宜模型。项目里有几层策略：模型网关按任务分层，RAG 先检索再生成，目录工具缓存减少重复调用，ContextWindowManager 裁剪历史，流式接口提升体验但不额外前置反思。生产里还应在网关层统计 token、预算、限额和告警。

---

**Q84：如果问“延迟主要来自哪里，怎么优化”，怎么答？**

A：主要来自模型生成、RAG rerank / embedding、工具调用和多轮 Agent 重试。优化方式是：流式输出降低首字等待；目录工具缓存；独立工具并行；RAG top_k 和 reranker 控制；复杂任务才启用 Plan-and-Execute 或 Multi-Agent；反思重试次数不要太高。

---

**Q85：如果问“Workflow 节点失败怎么处理”，怎么答？**

A：不可恢复错误可以用 `Command(goto="finalize")` 短路到最终节点，把错误写进状态；普通异常写入 `errors`，让后续节点或最终总结说明失败原因。这样不会让整个 API 因一个节点崩掉而完全无响应。

核心代码：`app/workflows/fulfillment/nodes.py`、`tests/unit/test_workflow_nodes.py`

## 第四部分：兜底扩展题

这一部分低频但可能被追问，面试前扫读即可。

---

**Q86：短期记忆和长期记忆分别是什么？**

A：

- 短期记忆：LangGraph checkpointer 保存当前会话消息历史，用 `thread_id` 区分会话。
- 长期记忆：LangGraph BaseStore 风格的长期存储，保存跨会话可复用摘要、偏好和订单处理结论。

核心代码：`app/memory/short_term.py`、`app/memory/long_term.py`

---

**Q87：为什么订单库存不放进 RAG？**

A：订单和库存是结构化、实时、高频变化的数据；RAG 更适合低频变化的非结构化知识。把实时库存放进向量库会有同步延迟、权限控制和过期风险。

---

**Q88：如果被问“项目最大亮点是什么”，怎么答？**

A：三个亮点：

1. 架构边界清楚：Workflow / Agent / RAG 各司其职。
2. 工程兜底完整：模型网关、工具包装、记忆、安全、评测、观测都有落点。
3. 能诚实区分当前实现和生产演进，不把 Demo 能力吹成已经完整上线。

---

**Q89：如果最后总结项目价值，怎么答？**

A：这个项目把供应链履约问题拆成可控 Workflow、灵活 Agent、可追溯 RAG 和模型治理四部分。它不是简单聊天 Demo，而是一个能展示业务决策、工具调用、规则依据、安全防护、记忆和评测观测的 AI 应用工程原型。

---

**Q90：为什么工具输出失败时返回可读字符串，而不是直接让异常抛给 LLM？**

A：工具异常直接抛出会中断整轮 Agent。包装层把 timeout、circuit open 这类工程错误转成可读字符串，模型可以基于“工具不可用”给出不确定性说明；真正程序异常仍然会被日志和上层异常处理捕获。

---

**Q91：InputGuardrails 为什么不能只靠一个“忽略之前指令”的正则？**

A：攻击会有多种形态：中文、英文、零宽字符、全角字符、ChatML 定界符、角色扮演、系统 prompt 提取、Jailbreak 关键词。当前先做 Unicode 归一化和压缩文本，再跑多组模式，能挡住一部分简单绕过。

---

**Q92：知识文档 ID 为什么要安全化？**

A：用户传入的 document_id 如果不处理，可能出现 `../../secret` 这类路径穿越。`knowledge_mgmt.py` 会把文档 ID 规范成安全文件名，只允许写入知识库目录下的 `.md` 文件。

核心代码：`app/api/routes/knowledge_mgmt.py` 的 `_safe_document_id()`

---

**Q93：为什么 Prometheus 指标不能只定义不打点？**

A：指标定义只是第一步，真正有用的是在请求、工具调用、缓存、安全事件发生的位置 inc / observe。当前项目已经有指标端点和部分缓存同步，但还要继续把 Agent 请求耗时、token、成本、guardrails 拦截等接到实际调用点。

---

**Q94：如果面试官问“你为什么没有继续堆功能”，怎么答？**

A：功能越多不等于项目越强。这个项目我更关注边界：实时数据和知识分开，确定流程和开放推理分开，在线质量门和离线评测分开，内部 Tool 和 MCP 扩展分开。很多能力可以继续做，但面试主线应该围绕 Workflow、Agent、RAG、模型网关、安全和评测这几个闭环。

---

**Q95：如果问“你最担心这个项目上线后的风险是什么”，怎么答？**

A：我最担心三类风险：第一是数据权限，不能让 Agent 看到不该看的订单或知识；第二是工具权限，不能让 Agent 自动执行发货、退款这类高风险动作；第三是知识过期，RAG 引用旧规则会误导决策。所以生产前要补 RBAC、租户隔离、审批流、知识版本和审计。

---

**Q96：你会怎么把这个项目推进到更生产级？**

A：我会按顺序做五件事：先接真实 ERP/WMS/数据库；再补租户和 RBAC；然后把 Guardrails 覆盖所有入口和工具；接着做知识库版本审批和回滚；最后把模型网关接入预算、fallback、限额和灰度。这个顺序是先数据可信，再权限安全，再模型治理。

---

**Q97：如果让你用一分钟讲项目亮点，怎么说？**

A：我会说三个亮点：第一，业务边界清楚，订单库存走结构化服务，规则知识走 RAG；第二，Agent 不是裸跑，有工具包装、反思质量门、Guardrails、记忆和 trace；第三，项目诚实地区分了当前实现和生产演进，知道哪些地方还需要补权限、数据接入和审计。

---

**Q98：如果面试官问“你会删掉什么”，怎么答？**

A：如果是为了面试演示，我不会删主链路，但会降低一些扩展模块的存在感，比如 Multi-Agent、MCP、长期记忆可以作为加分项，不作为第一主线。真正答辩时重点讲 Workflow、Agent、RAG、模型网关和安全评测，避免让面试官觉得项目散。

---

**Q99：如果问“这个项目最适合投什么岗位”，怎么答？**

A：最适合 AI 应用工程、Agent 工程、RAG 工程或后端大模型平台方向。它不是算法训练项目，而是把模型接入业务系统，处理工具、数据、记忆、安全、评测和可观测这些工程问题。

---

**Q100：如果问“为什么不用一个超级 prompt 解决所有事”，怎么答？**

A：超级 prompt 很难控制边界。订单库存是实时结构化数据，必须工具查；企业规则是可更新知识，应该 RAG 查；高风险流程要 Workflow 和 HITL 控制。Prompt 只能约束模型表达，不能替代数据源、权限、审计和业务流程。

---

**Q101：如果问“RAG 和 Agent 工具调用顺序谁决定”，怎么答？**

A：普通 ReAct Agent 里由模型根据问题和工具描述决定是否调用 `retrieve_knowledge`；Workflow 里由图的条件边决定是否进入知识检索；RAG API 则是用户直接调用检索服务。也就是说，不同入口的决策权不同：Agent 是动态选择，Workflow 是固定流程，RAG API 是显式检索。

---

**Q102：如果问“HybridService 的意图分类准不准，错了怎么办”，怎么答？**

A：当前是轻量规则/关键词分类，适合演示和路由原型，不应该说成强语义分类器。分类错了要有降级：Multi-Agent 失败可以回退 Agent，Agent 失败可以回退 Workflow；生产里可以加入 embedding 分类、小模型分类、人工反馈和路由命中率评估。

---

**Q103：如果问“知识库文档被投毒怎么办”，怎么答？**

A：当前工具输出层会净化明显的间接 Prompt Injection，但生产里不能只靠正则。还要做知识库上传权限、审核流程、版本管理、发布审批、来源记录和回滚。也可以在索引前做安全扫描，在线召回后再做工具输出净化。

---

**Q104：如果问“多租户怎么设计”，怎么答？**

A：所有订单、库存、知识文档、长期记忆、trace 都要带 `tenant_id`。API 层从 JWT / Session 解析用户和租户，Service 层所有查询都带租户过滤，RAG metadata filter 也要强制加租户条件。高风险操作还要按角色做 RBAC。

---

**Q105：如果问“权限系统现在实现了吗”，怎么答？**

A：当前没有完整 RBAC / 多租户权限系统，主要是架构上预留了边界。面试时要诚实：现在重点是 AI 决策链路和工程兜底，生产化必须补身份认证、租户隔离、角色权限、操作审计。

---

**Q106：如果问“真实发货工具接进来以后怎么保证安全”，怎么答？**

A：发货、退款、改地址这类工具要和查询工具分级。查询工具可以自动调用，写操作工具必须要求权限校验、人工确认、幂等 key、操作审计和失败补偿。Agent 只能生成建议或发起审批，不能直接绕过业务系统执行高风险动作。

---

**Q107：如果问“怎么处理模型供应商故障”，怎么答？**

A：模型网关可以扩展 fallback 策略：同 use_case 下配置多个 provider，主模型失败时切备用模型；同时要设置超时、重试、熔断和降级模板。当前代码已经把模型创建和业务调用隔离，后续在模型网关层加 fallback 不需要大改业务服务。

---

**Q108：如果问“为什么 max_reflection_retries 生产配置是 1，而代码默认是 2”，怎么答？**

A：代码默认值偏通用，配置才是生产入口。反思重试每多一次就是一次额外 Agent 调用，会增加成本和延迟。生产配置设为 1 是在质量兜底和响应成本之间做平衡；特别高风险场景可以单独调高。

---

**Q109：如果问“工具缓存 TTL 怎么定”，怎么答？**

A：按数据变化频率定。知识规则 10 分钟、替代 SKU 5 分钟这类可以接受短暂缓存；订单、库存、履约方案是实时数据，不缓存。真实生产还要结合业务 SLA，比如库存秒级变化就必须强一致或直接查源系统。

---

**Q110：如果问“RAG 的 chunk 怎么保证业务 metadata 有用”，怎么答？**

A：项目用 `BusinessMetadataEnricher` 给切片补 `category / chunk_id / source_path` 等元数据，后续检索可以按 category filter 或业务意图加权。这样比只有纯文本向量更适合企业知识库，因为规则类别和来源同样重要。

核心代码：`app/rag/knowledge_retrieval_service.py`

---

**Q111：如果问“为什么 AnswerBuilder 不直接让 RAG 服务返回大段原文”，怎么答？**

A：上层 Agent / Workflow 不需要一堆原文，它需要可操作证据摘要：结论、关键规则、建议动作和覆盖说明。AnswerBuilder 把召回结果整理成稳定结构，减少下游 prompt 噪声，也方便前端展示。

核心代码：`app/rag/rag_answer_builder.py`

---

**Q112：如果问“怎样判断 RAG 召回质量不好”，怎么答？**

A：看几个信号：命中文档 category 和问题意图不一致；reranker 前后 top 文档变化很大；答案引用规则但 source 不相关；Golden Dataset 的 must_contain 命不中；Ragas faithfulness / relevancy 下降。排查要从 query planner、召回、rerank、answer builder 分段看。

---

**Q113：如果问“为什么同步 Workflow 自动批准 HITL”，怎么答？**

A：这是为了兼容普通同步 HTTP 接口，保证它能返回一个完整结果。真正更接近生产审批的是流式 / hybrid 路径，遇到 HITL 会返回中断上下文等待人工 resume。面试时要说明这是接口模式差异，不是生产里真的自动批准所有风险。

---

**Q114：如果问“前端是不是生产可用”，怎么答？**

A：当前前端已经改成 React + TypeScript + Vite，适合作为生产化操作台继续扩展。它面向运营人员提供订单处理、人工审查队列和独立审查页；后端 API 与 Service 层保持稳定，后续可以继续接鉴权、审计日志和真实工单池。

---

**Q115：如果问“为什么前端要最新记录置顶”，怎么答？**

A：运营人员最关心本次订单处理结论，不是聊天历史。最新结果置顶、历史记录保留，更像工单系统，适合履约分析报告，而不是微信式闲聊气泡。

---

**Q116：如果问“接口层为什么要先限流再 Guardrails”，怎么答？**

A：恶意高频请求即使被 Guardrails 拦截，也会消耗 CPU 和日志资源。先限流可以挡住明显滥用，再做输入安全检查，最后才进入模型和工具链路，成本最低。

核心代码：`app/api/routes/agent.py`

---

**Q117：如果问“为什么要保留 MCP”，怎么答？**

A：项目内部 Agent 主要用本地 LangChain Tool，简单、低延迟、好测试；MCP 是扩展层，用来把能力标准化暴露给外部 IDE、其他 Agent 或远程服务。两者不是替代关系，一个偏内部实现，一个偏外部协议。

---

**Q118：如果问“当前测试覆盖最薄弱的是哪里”，怎么答？**

A：工程逻辑无 LLM 单测比较多，但端到端真实模型效果、Multi-Agent 长链路、Hybrid 路由准确率、生产级权限和安全审计还需要更多测试。这个回答要诚实，说明我知道单测和真实模型评测不是一回事。

---

**Q119：如果问“怎么做回归评测集”，怎么答？**

A：用 Golden Dataset 固定典型问题，标注 expected_tools、must_contain、must_not_hallucinate 和参考关键词。每次改 prompt、工具描述、RAG 参数或模型时跑一遍，先看硬指标，再抽样看 Langfuse trace 和 LLM-as-judge。

核心代码：`app/agents/quality/evaluation/golden_dataset.py`

---

**Q120：如果问“为什么不要把 prompt 全部硬编码在代码里”，怎么答？**

A：Prompt 是可迭代资产。硬编码会让调优、版本对比和回滚都很麻烦。当前 PromptRegistry 支持 Langfuse、YAML、本地内置三层降级，既能在线管理，也能保证本地开发和服务兜底。

---

**Q121：如果问“你怎么向非技术业务方解释这个项目”，怎么答？**

A：我会说它是一个履约决策助手：它会查订单、查库存、查规则，告诉你这个订单能不能发、风险是什么、缺货怎么处理、依据来自哪条规则。但它不会直接替你发货或退款，高风险动作仍然要人审批。

---

**Q122：如果问“这个项目和普通客服机器人最大区别是什么”，怎么答？**

A：普通客服机器人主要回答文本问题；这个项目围绕履约决策，需要实时工具、结构化数据、规则依据、风险审批和审计。重点不是聊天自然，而是建议能不能被业务人员信任和追溯。

---

**Q123：如果问“未来接真实数据库会怎么改”，怎么答？**

A：优先替换 repository 层和 service 依赖，不改 Agent 工具接口。比如订单工具仍然叫 `analyze_order(order_id)`，只是底层从本地数据改成 MySQL / ERP API。这样 Agent prompt、工具描述和上层 API 基本不受影响。

---

**Q124：如果问“为什么要把订单、库存、知识三个服务拆开”，怎么答？**

A：它们的数据性质不同：订单是交易对象，库存是实时资源，知识是规则文档。拆开后各自可以独立替换数据源、缓存策略和测试替身，也避免一个大 service 既查数据库又管 RAG，边界混乱。

---

**Q125：如果问“你怎么处理用户问题缺少订单号或 SKU”，怎么答？**

A：工具层会要求必要参数，Multi-Agent 规则降级里如果缺 SKU 会跳过 SKU 粒度工具。更好的生产做法是让 Agent 先追问缺失信息，而不是乱调工具。这个可以通过工具 description、prompt 和参数校验共同控制。

---

**Q126：如果问“这个项目有哪些地方用了异步”，怎么答？**

A：Agent 流式输出、Workflow SSE、RAG 异步检索、工具 async 调用、知识库后台重建都涉及异步。异步主要是为了避免长任务阻塞请求线程，让前端能看到过程或快速收到状态。

---

**Q127：如果问“你怎么设计日志里的 trace_id”，怎么答？**

A：请求进来时从 header 读取或生成 `X-Trace-Id`，放进 ContextVar，日志自动带上这个 ID，响应也回写。这样一次前端请求、后端日志、Langfuse trace 和错误排查能串起来。

核心代码：`app/core/logging.py`

---

**Q128：如果问“你会如何准备现场演示”，怎么答？**

A：我会准备三条路径：一个简单订单走 Workflow，看固定流程和 HITL；一个开放问题走 Agent，看工具调用和流式输出；一个缺货规则问题走 RAG，看命中文档和依据。最后展示一两个 guardrails 或维度错误排查点，体现工程兜底。

---

**Q129：如果问“这个项目最容易被误解的点是什么”，怎么答？**

A：最容易被误解成“多 Agent 越多越好”。其实 Multi-Agent 只是复杂跨域问题的扩展，不是第一主线。真正核心是业务边界清楚、数据可信、工具可控、输出有依据。

---

**Q130：如果问“你希望面试官记住什么”，怎么答？**

A：希望记住我不是只会接模型 API，而是能把模型放进一个后端系统里：知道哪些该用规则，哪些该用 Agent，哪些该用 RAG，也知道安全、成本、延迟、权限和评测这些真实落地问题。

---

## 快速复习路线

面试前按这条线看，不用逐行背：


1. `README.md`

2. `app/api/routes/agent.py`

3. `app/agents/runtime/agent_service.py`

4. `app/agents/orchestration/react_agent.py`

5. `app/agents/tools/factory.py`

6. `app/agents/tools/wrapper.py`

7. `app/agents/tools/guardrails.py`

8. `app/rag/knowledge_retrieval_service.py`

9. `app/rag/rag_query_planner.py`

10. `app/rag/reranker.py`

11. `app/application/workflow/workflow_service.py`

12. `app/workflows/fulfillment/nodes.py`

13. `app/agents/orchestration/plan_execute.py`

14. `app/agents/orchestration/multi_agent.py`

15. `app/infrastructure/llm/model_gateway.py`

16. `app/memory/long_term.py`


最后背这一句：**订单库存走结构化服务，规则知识走 RAG，关键流程走 Workflow，开放追问走 Agent，跨域复杂问题走 Multi-Agent，所有模型能力都要经过安全、记忆、观测和评测的工程边界。**
