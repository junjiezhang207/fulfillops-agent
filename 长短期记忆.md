# Multiship 长短期记忆实现说明

这份文档用于说明 Multiship 项目里的长短期记忆是怎么实现的，适合面试时直接讲解。

项目里的记忆不是单纯把聊天内容保存起来，而是分成两层：

- **短期记忆**：保存当前会话上下文，让 Agent 能连续对话。
- **长期记忆**：保存跨会话可复用的业务信息，例如客户偏好、订单处理结论、历史决策摘要。

整体设计是：

```text
用户请求
  ↓
AgentService
  ↓
短期记忆：LangGraph Checkpointer
  ├─ RedisSaver：Redis 可用时持久化会话消息
  └─ MemorySaver：Redis 不可用时自动降级
  ↓
长期记忆召回：按 session / order / preference namespace 检索
  ↓
Agent 推理与工具调用
  ↓
长期记忆沉淀：筛选有价值内容
  ↓
记忆治理：脱敏、去重、冲突检测、优先级处理
  ↓
长期记忆存储
  ├─ 本地开发：SQLite
  └─ 企业生产：MySQL + Milvus
```

## 1. 相关文件

| 文件 | 作用 |
| --- | --- |
| [app/memory/__init__.py](app/memory/__init__.py) | 记忆模块统一导出入口。 |
| [app/memory/short_term.py](app/memory/short_term.py) | 短期记忆实现，优先 Redis Checkpointer，失败降级 MemorySaver。 |
| [app/memory/long_term.py](app/memory/long_term.py) | 长期记忆 Store，实现 SQLite 和 MySQL + Milvus 两种后端。 |
| [app/memory/governance.py](app/memory/governance.py) | 长期记忆治理层，负责脱敏、去重、冲突检测和写入决策。 |
| [app/services/agent_service.py](app/services/agent_service.py) | Agent 主服务，负责记忆召回、注入、沉淀和治理写入。 |
| [app/services/session_memory_service.py](app/services/session_memory_service.py) | Hybrid Routing 演示用的轻量会话缓存，不是正式 Agent 长短期记忆主链路。 |
| [app/core/config.py](app/core/config.py) | 统一读取 Redis、MySQL、Milvus、长期记忆等配置。 |
| [app/core/rate_limiter.py](app/core/rate_limiter.py) | Redis 限流实现，展示 Redis 在工程层面的另一个用途。 |
| [docker-compose.milvus.yml](docker-compose.milvus.yml) | 本地启动 MySQL + Milvus 的 Docker Compose。 |
| [.env.example](.env.example) | 长短期记忆相关环境变量示例。 |
| [tests/unit/test_memory_store.py](tests/unit/test_memory_store.py) | 长期记忆 Store 单元测试。 |
| [tests/unit/test_memory_governance.py](tests/unit/test_memory_governance.py) | 记忆治理、冲突、安全脱敏测试。 |
| [tests/unit/test_agent_memory_policy.py](tests/unit/test_agent_memory_policy.py) | Agent 长期记忆写入策略测试。 |

## 2. 短期记忆

短期记忆解决的是“同一个会话里，Agent 能不能记住前面几轮对话”的问题。

在本项目中，短期记忆由 [app/memory/short_term.py](app/memory/short_term.py) 实现：

```text
create_redis_checkpointer()
  ↓
尝试连接 Redis
  ↓
成功：使用 LangGraph RedisSaver
失败：降级为 LangGraph MemorySaver
```

### 2.1 技术选型

| 能力 | 实现 |
| --- | --- |
| 会话上下文保存 | LangGraph Checkpointer |
| 首选后端 | Redis |
| 降级后端 | MemorySaver |
| TTL | 默认 24 小时 |
| 多实例共享 | Redis 可支持 |
| 服务重启恢复 | Redis 可支持 |

短期记忆的核心函数：

- [create_redis_checkpointer](app/memory/short_term.py)
- [get_session_history_from_redis](app/memory/short_term.py)

### 2.2 Agent 中的接入位置

Agent 在初始化时创建 checkpointer，位置在 [app/services/agent_service.py](app/services/agent_service.py)：

```text
AgentService.__init__()
  ↓
create_redis_checkpointer(settings.redis_url)
  ↓
create_agent(..., checkpointer=self._checkpointer)
```

这意味着同一个 `session_id` 会被映射成 LangGraph 的 `thread_id`，后续多轮对话可以延续上下文。

## 3. 长期记忆

长期记忆解决的是“跨会话、跨订单、跨请求仍然有价值的信息如何保存和复用”的问题。

长期记忆由 [app/memory/long_term.py](app/memory/long_term.py) 实现，统一遵循 LangGraph `BaseStore` 接口。

### 3.1 当前支持的两种后端

| 后端 | 使用场景 | 说明 |
| --- | --- | --- |
| SQLiteLongTermMemoryStore | 本地开发、面试演示、轻量运行 | 默认启用，落盘到 `storage/long_term_memory.sqlite3`。 |
| MySQLMilvusLongTermMemoryStore | 企业级生产架构 | MySQL 保存结构化记忆和审计字段，Milvus 保存语义向量索引。 |

创建入口：

- [create_long_term_memory_store](app/memory/long_term.py)

默认配置：

```env
LONG_TERM_MEMORY_BACKEND=sqlite
LONG_TERM_MEMORY_DB_PATH=storage/long_term_memory.sqlite3
```

企业级配置：

```env
LONG_TERM_MEMORY_BACKEND=mysql_milvus
MYSQL_URL=mysql+pymysql://root:root@localhost:3306/multiship_agent
LONG_TERM_MEMORY_MYSQL_URL=mysql+pymysql://root:root@localhost:3306/multiship_agent
LONG_TERM_MEMORY_MILVUS_COLLECTION=long_term_memory_vectors_1024
LONG_TERM_MEMORY_VECTOR_DIMENSION=1024
MILVUS_URI=http://localhost:19530
```

注意：本项目长期记忆不使用 PostgreSQL，生产表达是 **MySQL + Milvus**。

### 3.2 为什么长期记忆用 MySQL + Milvus

长期记忆同时有两类需求：

- 精确管理：namespace、key、TTL、importance、access_count、审计记录、删除标记。
- 语义检索：根据当前问题找相似的历史偏好、订单结论、处理经验。

所以项目采用职责拆分：

| 组件 | 职责 |
| --- | --- |
| MySQL | 权威数据源，保存结构化记忆、TTL、访问次数、版本、审计记录。 |
| Milvus | 可重建的向量索引，负责相似记忆召回。 |
| Embedding 模型 | 把长期记忆的文本表示转成向量。 |

这样做的好处是：

- MySQL 中的数据可审计、可回滚、可做权限治理。
- Milvus 只保存可重建索引，出问题可以从 MySQL 重建。
- 结构化数据和向量检索职责分离，更接近企业系统设计。

## 4. 长期记忆保存什么

Agent 不会把每一句闲聊都写入长期记忆。写入前会先判断这条信息是否有业务复用价值。

主要记忆类型在 [app/services/agent_service.py](app/services/agent_service.py) 里生成：

| memory_type | 含义 | 示例 |
| --- | --- | --- |
| `user_preference` | 用户或客户偏好 | “VIP 客户不接受替代 SKU。” |
| `order_decision` | 有工具证据支撑的订单处理结论 | “订单 SO202502140003 因海外仓缺货进入人工审查。” |
| `conversation_summary` | 高价值会话摘要 | “用户重点关注 SLA 和跨仓调拨风险。” |

### 4.1 namespace 设计

长期记忆通过 namespace 隔离不同作用域：

| namespace | 含义 |
| --- | --- |
| `("sessions", session_id, "preferences")` | 当前会话中的用户偏好。 |
| `("customers", customer_id, "preferences")` | 客户级偏好。 |
| `("orders", order_id)` | 订单级历史决策。 |
| `("sessions", session_id)` | 会话摘要。 |
| `("global", "preferences")` | 全局默认偏好。 |

这种设计能支持：

- 同一个会话里快速记住上下文。
- 同一个客户跨会话复用偏好。
- 同一个订单后续追问时召回历史处理结论。
- 全局规则与客户偏好发生冲突时做优先级判断。

## 5. 长期记忆召回流程

Agent 每轮执行前会先召回长期记忆，位置在 [app/services/agent_service.py](app/services/agent_service.py)：

```text
用户问题
  ↓
_message_with_memory(session_id, message)
  ↓
_search_memories(session_id, message)
  ↓
检索 user_preference / session summary / order_decision
  ↓
过滤 superseded / deleted / rejected
  ↓
渲染为【长期记忆参考】
  ↓
拼接到当前问题前面
```

简化后的 prompt 形态：

```text
【长期记忆参考】
- 用户偏好：不接受替代 SKU
- 历史订单结论：SO202502140003 因库存不足进入人工审查

【当前问题】
帮我判断这个订单是否可以发货
```

这让模型不是凭空回答，而是带着项目沉淀的业务上下文进行分析。

## 6. 长期记忆写入流程

Agent 每轮回答结束后，会判断是否需要写入长期记忆：

```text
Agent 回复完成
  ↓
_remember_turn()
  ↓
_build_memory_candidates()
  ↓
计算 memory_write_score
  ↓
低价值闲聊：不写入
高价值业务信息：生成候选记忆
  ↓
MemoryGovernanceService.govern_and_write()
  ↓
写入长期记忆 Store
```

判断是否值得写入的因素：

- 是否有明确偏好。
- 是否提到订单号。
- 是否调用了工具。
- 是否涉及缺货、风险、人工复核、承诺、SLA 等业务信号。
- 用户消息是否过短、是否只是普通寒暄。

对应测试：

- [tests/unit/test_agent_memory_policy.py](tests/unit/test_agent_memory_policy.py)

## 7. 记忆治理

长期记忆治理由 [app/memory/governance.py](app/memory/governance.py) 实现。

它解决的是两个问题：

1. **记忆安全**：不能把手机号、邮箱、API Key、认证头等敏感信息长期保存。
2. **记忆冲突**：不能让新旧偏好互相打架，例如“接受替代 SKU”和“不接受替代 SKU”同时生效。

### 7.1 治理流程

```text
候选长期记忆
  ↓
_sanitize()
  ↓
敏感信息脱敏 / 丢弃高风险字段
  ↓
_facts_for()
  ↓
从自然语言偏好中抽取结构化事实
  ↓
_find_conflicts_and_duplicates()
  ↓
检查同 scope、客户 scope、global scope 是否有冲突或重复
  ↓
生成 GovernanceDecision
  ↓
govern_and_write() 应用写入操作
```

### 7.2 记忆安全

当前会脱敏或丢弃：

| 类型 | 处理方式 |
| --- | --- |
| 邮箱 | 替换为 `[EMAIL]` |
| 手机号 | 替换为 `[PHONE]` |
| 身份证号 | 替换为 `[ID_CARD]` |
| API Key / secret | 替换为 `[SECRET_KEY]` 或 `[SECRET]` |
| `authorization` | 字段丢弃 |
| `cookie` | 字段丢弃 |
| `raw_prompt` | 字段丢弃 |
| `raw_messages` | 字段丢弃 |

这样可以保证长期记忆只保存对业务有用的信息，而不是保存完整原始请求。

对应测试：

- [tests/unit/test_memory_governance.py](tests/unit/test_memory_governance.py)

### 7.3 记忆冲突

当前主要治理 `user_preference` 类型的冲突。

会从偏好文本中抽取结构化事实，例如：

| 原始偏好 | 结构化事实 |
| --- | --- |
| “不接受替代 SKU” | `substitute_sku = reject` |
| “可以接受替代 SKU” | `substitute_sku = accept` |
| “优先时效” | `fulfillment_priority = speed` |
| “优先成本” | `fulfillment_priority = cost` |
| “先人工确认” | `review_policy = manual_first` |
| “自动放行” | `review_policy = auto_release` |

如果新旧事实完全一致，视为重复记忆，不再新增一条，而是合并证据：

```text
action = merge_evidence
confirmation_count + 1
last_confirmed_at = 当前时间
```

如果新旧事实冲突，则按优先级处理。

### 7.4 优先级规则

优先级不是重要性分数，而是冲突时谁更有权威。

| 来源 | 优先级 |
| --- | --- |
| business_rule | 100 |
| session 明确指令 | 90 |
| customer 长期偏好 | 80 |
| global 默认偏好 | 60 |
| 普通历史经验 | 40 |

处理结果：

| 情况 | 结果 |
| --- | --- |
| 新记忆优先级更高或相同 | 旧记忆标记为 `superseded`，新记忆写入。 |
| 旧记忆优先级更高 | 新记忆不写入，返回 `requires_review`。 |
| 新旧事实完全一致 | 合并证据，不重复写入。 |
| 记忆含敏感信息但仍有业务价值 | 脱敏后写入。 |
| 纯敏感信息且没有业务价值 | `reject_unsafe`。 |

### 7.5 为什么 govern 和 write 分开

治理层分成两个方法：

| 方法 | 是否写库 | 用途 |
| --- | --- | --- |
| `govern()` | 不写库 | 只判断安全、重复、冲突，生成操作计划。 |
| `govern_and_write()` | 写库 | 按操作计划写入 Store。 |

这样设计是为了避免“判断冲突时已经把旧记忆改掉，但新记忆写入失败”的半成功状态。

企业级生产里，`supersede_old + put_new` 应该放进同一个 MySQL 事务，Milvus 作为可重建索引异步同步。

## 8. Redis 在项目里的两个作用

Redis 在本项目里不是长期记忆存储，而是用于短期和工程控制。

| 场景 | 文件 | 作用 |
| --- | --- | --- |
| 短期记忆 | [app/memory/short_term.py](app/memory/short_term.py) | LangGraph RedisSaver 保存会话 checkpoint。 |
| 接口限流 | [app/core/rate_limiter.py](app/core/rate_limiter.py) | 用 Redis 固定窗口计数，保护 LLM 接口成本。 |

Redis 不可用时：

- 短期记忆降级为 `MemorySaver`。
- 限流降级为进程内 `RateLimiter`。

## 9. Hybrid Routing 会话缓存说明

[app/services/session_memory_service.py](app/services/session_memory_service.py) 里也有一个 `SessionMemoryService`，但它不是正式的 Agent 长短期记忆主链路。

它的定位是：

- 给 `/hybrid/*` 演示路由使用。
- 在内存里保存当前订单、路由轮次、少量缓存字段。
- 支持 TTL 和过期清理。
- 不负责跨会话长期沉淀。

面试讲解时可以这样区分：

```text
Agent 正式记忆：
  短期：LangGraph Checkpointer + Redis
  长期：SQLite / MySQL + Milvus

Hybrid 演示缓存：
  SessionMemoryService 进程内缓存，只服务路由演示。
```

## 10. 启动 MySQL + Milvus

本地启动：

```powershell
docker compose -f docker-compose.milvus.yml up -d
```

该 compose 会启动：

- MySQL 8.4
- Milvus standalone
- etcd
- MinIO

配置长期记忆切换到 MySQL + Milvus：

```env
LONG_TERM_MEMORY_BACKEND=mysql_milvus
MYSQL_URL=mysql+pymysql://root:root@localhost:3306/multiship_agent
LONG_TERM_MEMORY_MYSQL_URL=mysql+pymysql://root:root@localhost:3306/multiship_agent
LONG_TERM_MEMORY_MILVUS_COLLECTION=long_term_memory_vectors_1024
LONG_TERM_MEMORY_VECTOR_DIMENSION=1024
MILVUS_URI=http://localhost:19530
```

如果没有启动 MySQL + Milvus，默认可以继续使用 SQLite：

```env
LONG_TERM_MEMORY_BACKEND=sqlite
LONG_TERM_MEMORY_DB_PATH=storage/long_term_memory.sqlite3
```

## 11. 面试讲解话术

可以这样介绍这个项目的记忆系统：

> Multiship 的记忆分为短期和长期。短期记忆用 LangGraph Checkpointer 保存当前会话上下文，优先落 Redis，这样服务重启或多实例部署时会话可以恢复；Redis 不可用时降级为 MemorySaver，保证演示不中断。
>
> 长期记忆用于保存跨会话可复用的业务信息，比如客户是否接受替代 SKU、某个订单为什么进入人工审查、用户更关注时效还是成本。本地开发默认用 SQLite 落盘，生产设计是 MySQL + Milvus：MySQL 保存结构化记忆、TTL、访问次数和审计信息，Milvus 保存可重建的语义向量索引。
>
> Agent 每轮执行前会按 session、order、preference namespace 检索长期记忆，并注入到当前问题中；每轮结束后只把有业务价值的信息沉淀为长期记忆。写入前会经过治理层，做敏感信息脱敏、重复合并、偏好冲突检测和优先级处理，避免旧偏好和新偏好同时生效。
>
> 所以这个记忆系统不是简单聊天记录，而是一个面向电商履约场景的上下文治理能力。

## 12. 当前能力边界

已经实现：

- Redis 短期记忆。
- Redis 不可用自动降级 MemorySaver。
- SQLite 长期记忆落盘。
- MySQL + Milvus 长期记忆生产后端。
- 长期记忆 namespace 隔离。
- 长期记忆 TTL、importance、access_count。
- Agent 执行前长期记忆召回。
- Agent 执行后长期记忆沉淀。
- 低价值闲聊过滤。
- 用户偏好、订单决策、会话摘要三类记忆。
- 敏感信息脱敏。
- 记忆重复合并。
- 偏好冲突检测。
- 旧记忆 `superseded` 状态。
- 高优先级冲突进入 `requires_review`。
- 相关单元测试覆盖。

仍可继续增强：

- 把 MySQL 版本的 `supersede + put_new` 做成显式事务。
- 给记忆治理增加后台可视化页面。
- 增加人工确认记忆冲突的接口。
- 增加长期记忆删除、导出和审计查询 API。
- 对更多业务事实做结构化抽取，例如仓库偏好、承运商偏好、SLA 策略。
- 将 Milvus 索引重建做成独立维护脚本。

## 13. 测试入口

记忆相关测试：

```powershell
.\.venv\Scripts\python.exe -m pytest tests\unit\test_memory_store.py tests\unit\test_memory_governance.py tests\unit\test_agent_memory_policy.py -q
```

完整 unit 测试：

```powershell
.\.venv\Scripts\python.exe -m pytest tests\unit -q
```

