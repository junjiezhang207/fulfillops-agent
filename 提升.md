# 面试使用提升建议

> 目标不是继续堆技术名词，而是让面试官相信：这个项目有真实业务边界、有工程落地能力、有可验证结果，也能清楚解释为什么这么设计。

---

## 一句话结论

这个项目现在的技术覆盖已经很强：FastAPI、LangGraph、ReAct Agent、Multi-Agent、RAG、Memory、Guardrails、Prompt 管理、评测和可观测都有。  

面试视角下，下一步最值得提升的不是再加更多 Agent，而是补齐三件事：

1. **真实企业数据流**：让订单、库存、知识、异常场景像真实系统一样进入平台。
2. **可量化演示结果**：让面试官看到延迟、命中率、成功率、成本、评测分数。
3. **生产化取舍说明**：明确哪些是 Demo 实现，哪些是生产替换方案。

---

## P0：最优先提升

### 1. 准备一套完整的面试演示数据

当前项目的架构很完整，但如果演示时只用几条 demo 数据，会显得“技术很大，业务很薄”。建议准备一套合成但真实感强的数据。

建议数据规模：

| 数据类型 | 建议数量 | 用途 |
|---|---:|---|
| 订单 | 5000 到 20000 单 | 展示企业数据接入、订单分析、缺货场景 |
| 库存记录 | 10000 到 50000 条 | 展示多仓库存判断、库存不足、跨仓建议 |
| SKU | 1000 到 5000 个 | 展示替代 SKU、商品维度分析 |
| 仓库 | 20 到 50 个 | 展示区域仓、跨仓调拨 |
| RAG 规则文档 | 50 到 100 篇 | 展示规则检索、SOP 问答 |
| 异常订单 | 200 到 1000 单 | 展示 HITL、风险识别、人工审批 |

重点不是数量越大越好，而是数据之间要能对上：

- 订单里的 SKU 要能在库存表查到。
- 一部分订单要故意缺货。
- 一部分订单要高优先级、临近 SLA、金额较高。
- RAG 文档里要能检索到对应处理规则。
- 同一个订单要能串起：订单分析 → 库存判断 → RAG 规则 → Workflow 决策 → Agent 解释。

面试时可以这样讲：

> 我把数据分成两类：订单、库存这类强结构化实时数据走企业数据接入层；履约规则、售后政策、平台 SLA 这类非结构化知识进入 RAG。这样避免把 RAG 当数据库用，也更符合真实企业系统的数据边界。

---

### 2. 做一个“标准演示剧本”

项目能力很多，面试现场不能靠临时点页面。建议准备 3 条固定演示链路，每条 2 到 3 分钟。

#### 演示 1：标准订单履约

输入一个库存充足订单：

```text
SO-DEMO-0001 能正常发货吗？
```

展示点：

- Workflow 直接走订单分析和库存分析。
- 不触发 RAG，延迟低。
- 输出可履约结论、涉及 SKU、仓库库存。

面试价值：

> 说明不是所有问题都交给 Agent，标准流程用 Workflow 更稳定、更低延迟。

#### 演示 2：缺货订单 + RAG 规则

输入一个缺货订单：

```text
SO-DEMO-0238 缺货时应该怎么处理？
```

展示点：

- 库存不足。
- 自动检索缺货处理 SOP。
- 给出跨仓、拆单、替代 SKU 或人工审批建议。
- 展示命中的 RAG 文档片段。

面试价值：

> 说明系统能把实时业务数据和规则知识结合起来，不是纯聊天机器人。

#### 演示 3：复杂问题走 Agent / Multi-Agent

输入一个复合问题：

```text
分析 SO-DEMO-1024 的履约风险，给出库存、时效、替代 SKU 和人工审批建议。
```

展示点：

- Agent 调用多个工具。
- 必要时并行查询库存和知识。
- 输出风险原因和下一步动作。
- 展示 trace / tool call / metrics。

面试价值：

> 说明 Agent 只用于步骤不固定、需要自主决策的问题，而不是替代所有业务流程。

---

### 3. 补一个“演示看板”

现在前端有多个页面，但面试官最希望一眼看到项目效果。建议新增一个“面试演示”页或 Dashboard。

建议展示：

| 区域 | 内容 |
|---|---|
| 数据概览 | 订单数、库存记录数、SKU 数、知识文档数 |
| 今日分析 | Workflow 次数、Agent 次数、成功率、平均延迟 |
| 风险分布 | 可履约、缺货、高风险、需人工审批 |
| RAG 效果 | 平均召回耗时、top-k 命中、重排前后分数 |
| Agent 质量 | 工具调用次数、反思重试次数、缓存命中率 |
| 最新 trace | 最近一次订单的执行链路 |

面试价值：

> 面试官不一定有时间读代码，看板能快速把“我做了什么”和“效果怎么样”讲清楚。

---

### 4. 把本地 JSON 存储升级或明确标注为 Demo 存储

当前企业数据默认落盘到 `storage/enterprise_data`，适合学习和演示，但生产上不够真实。

建议做两种选择之一：

#### 方案 A：真的接 PostgreSQL / MySQL

最有面试价值。实现订单表、库存表、数据源表，Repository 接口不变，只替换底层实现。

推荐表：

- `enterprise_sources`
- `orders`
- `order_items`
- `inventory_snapshots`
- `import_batches`

面试价值：

> 我把领域服务和存储实现隔离，Demo 用本地 JSON，生产可以切到数据库，业务层不用改。

#### 方案 B：不接数据库，但文档写清楚替换方案

如果时间不够，至少在 README 或架构文档里写清楚：

- 当前 JSON 存储只用于本地演示。
- 生产建议用 PostgreSQL/MySQL 存订单和库存。
- Redis 用于缓存和会话。
- Milvus/pgvector 用于向量检索。
- 对外仍通过 Repository 抽象访问。

---

### 5. 补全可量化评测报告

项目已经有 Ragas、DeepEval、Langfuse、Prometheus，但面试时需要“结论型数字”。

建议生成一个 `评测报告.md`，包含：

| 指标 | 示例 |
|---|---|
| Workflow 平均耗时 | 320ms，无 LLM 路径 |
| Agent 平均耗时 | 4.8s，含 2 到 4 次工具调用 |
| RAG top-3 命中率 | 86% |
| RAG answer relevancy | 0.82 |
| DeepEval 通过率 | 92% |
| Guardrails 注入拦截率 | 直接注入 100%，间接注入 95% |
| 工具缓存命中率 | 知识检索 40% 到 70% |
| 并发压测 | 50 并发成功率 99% |

哪怕数字来自合成数据，也可以说清楚：

> 这是基于 synthetic data 的离线评测，主要用于验证工程链路和回归稳定性，不代表真实企业生产指标。

---

## P1：增强面试说服力

### 6. 增加“导入批次”和“校验报告”

真实企业上传数据时，不只是“上传成功”，还需要知道：

- 这次上传了多少条。
- 成功多少条。
- 失败多少条。
- 哪些字段不合法。
- 是否覆盖旧数据。
- 数据来自哪个批次。

建议新增：

```text
import_batch_id
source_id
data_type
file_name
total_count
success_count
failed_count
created_at
error_samples
```

前端上传后展示：

- 导入批次号
- 成功 / 失败数量
- 前 20 条错误样例
- 可下载错误明细

面试价值：

> 这会明显比“贴 JSON 调接口”更像真实企业数据接入。

---

### 7. 增加异步任务队列设计

当前 HTTP 同步调用适合演示，但真实业务里大文件导入、RAG 重建、批量 Agent 分析都不应该阻塞请求。

建议架构演进：

```text
FastAPI 接收请求
  → 写入 import_jobs / analysis_jobs
  → 后台 Worker 执行
  → 前端轮询或 WebSocket/SSE 查看进度
```

可选技术：

- Redis Queue / ARQ
- Celery
- Dramatiq
- Kafka + Consumer

面试时重点讲取舍：

> 当前项目为了本地演示采用同步接口；生产环境中，批量导入和长链路 Agent 分析会改成异步任务，避免 HTTP 超时，也方便失败重试和进度追踪。

---

### 8. 增加权限和多租户隔离说明

如果项目定位企业 SaaS 或供应链平台，多租户是非常关键的面试点。

建议至少在设计文档里补：

- `tenant_id` 贯穿订单、库存、知识库、会话、记忆。
- 每个租户独立数据源。
- RAG 检索必须带租户过滤条件。
- Long-term memory 按租户和用户 namespace 隔离。
- Prompt 可以按行业 / 租户覆盖。
- API Key 或 JWT 鉴权。

面试价值：

> 多租户隔离能体现你不是只做了单机 Demo，而是考虑了 SaaS 化边界。

---

### 9. 强化 Prompt 和 Agent 可审计性

现在项目已有 PromptRegistry、trace、ReActCycle。建议再补一个前端展示或文档说明：

- 当前使用的 Prompt 版本。
- 本次 Agent 调用了哪些工具。
- 每个工具输入输出摘要。
- 最终结论引用了哪些数据。
- 反思评分是多少，是否重试。

面试价值：

> Agent 系统最大的问题是不可控和不可解释。能审计工具链路和 Prompt 版本，是工程化亮点。

---

### 10. 增加失败场景演示

面试官很喜欢问失败模式。建议主动准备几个失败场景：

| 场景 | 系统表现 |
|---|---|
| 订单不存在 | Workflow 短路到 finalize，返回明确错误 |
| 库存服务超时 | 工具 timeout + retry + circuit breaker |
| RAG 无命中 | 返回低置信度，并提示人工确认 |
| 用户 prompt injection | InputGuardrails 拦截 |
| 知识库文档含恶意指令 | ToolOutputSanitizer 净化 |
| LLM 不可用 | Agent/Workflow 返回 503，其他接口可用 |
| Redis 不可用 | checkpointer 降级 MemorySaver |

面试价值：

> 主动讲失败模式，会比只展示成功路径更像真实工程经验。

---

## P2：锦上添花

### 11. 增加 Docker Compose 一键启动

建议提供：

```text
docker-compose.yml
```

包含：

- FastAPI
- React 前端
- Redis
- PostgreSQL 或 MySQL
- Milvus 或 Qdrant
- Prometheus

面试价值：

> 面试官可以更容易跑起来，也体现部署意识。

---

### 12. 增加 OpenAPI 示例和 Postman Collection

建议补：

- `docs/postman_collection.json`
- `docs/api_examples.md`
- 常见接口请求样例
- 典型订单 ID 列表

面试价值：

> 面试官或自己演示时不用临时拼请求，减少翻车概率。

---

### 13. 增加架构决策记录 ADR

建议新增 `docs/adr/`：

```text
0001-why-langgraph.md
0002-why-rag-not-store-orders.md
0003-why-hybrid-routing.md
0004-why-tool-cache-split-realtime-and-catalog.md
0005-why-json-storage-for-demo.md
```

每篇写：

- 背景
- 选择
- 替代方案
- 取舍
- 后续演进

面试价值：

> ADR 能体现你不是“会用框架”，而是能做架构取舍。

---

### 14. 增加成本控制说明

Agent 项目绕不开成本问题。建议补一个“LLM 成本控制”小节：

- 简单问题走 Workflow，减少 LLM 调用。
- RAG 上下文压缩，减少 prompt token。
- 工具缓存只缓存目录数据，不缓存实时库存。
- Prompt 版本管理，减少无效重试。
- 自反思有最大重试次数。
- 可选低成本模型 DeepSeek / Kimi / 通义。

面试价值：

> 企业面试很看重成本意识。不是能调大模型就行，还要知道怎么省钱和控风险。

---

### 15. 增加安全合规说明

建议说明：

- API Key 不入库或加密存储。
- 日志脱敏订单手机号、地址、客户姓名。
- LLM 输入前做敏感字段裁剪。
- RAG 文档按租户隔离。
- 工具输出做间接注入净化。
- 生产环境关闭调试接口和详细错误栈。

面试价值：

> 供应链系统会碰到企业经营数据，安全和权限是很实际的问题。

---

## 建议的两周提升路线

### 第 1 到 2 天：数据和演示链路

- 生成 5000 单订单、10000 条库存、50 篇规则文档。
- 准备 3 个固定订单 ID：正常、缺货、高风险。
- 写好演示脚本。

### 第 3 到 4 天：前端演示增强

- 增加“面试演示”页。
- 展示数据概览、风险分布、最新分析结果。
- 企业上传页保留 CSV / JSON 上传和预览。

### 第 5 到 7 天：评测报告

- 跑单元测试、RAG 评测、Guardrails 测试。
- 记录平均耗时、通过率、命中率。
- 写 `评测报告.md`。

### 第 8 到 10 天：生产化补强

- 明确 JSON 存储是 Demo 实现。
- 写 PostgreSQL / Redis / Milvus 替换方案。
- 补异步任务队列设计。

### 第 11 到 14 天：面试材料整理

- 更新 README 的项目亮点。
- 补 ADR。
- 整理简历项目描述。
- 准备 10 个高频追问答案。

---

## 简历描述建议

可以把项目写成这样：

> 基于 FastAPI、LangGraph、LangChain、LlamaIndex 构建全域电商供应链智能履约平台，支持订单分析、库存判断、缺货规则检索、ReAct Agent 对话、Multi-Agent 专家协作和 HITL 人工审批。系统将强结构化订单/库存数据与非结构化履约规则分层接入，通过 Hybrid Router 在 Workflow、Agent、Multi-Agent 之间动态路由；同时实现工具熔断重试、Prompt Injection 双层防护、长期记忆、Langfuse/Prometheus 可观测和 Ragas/DeepEval 评测闭环。

如果简历需要更工程化一点：

> 负责设计并实现供应链履约 AI Agent 平台：使用 Repository 模式隔离企业订单/库存数据源，使用 LangGraph StateGraph 编排可中断 Workflow，接入混合检索 RAG 和业务重排策略，并通过结构化日志、trace_id、Prometheus 指标和 LLM-as-judge 评测保障链路可观测与可回归。

---

## 面试时最该强调的 5 个点

### 1. 不是所有问题都用 Agent

标准流程用 Workflow，复杂问题才用 Agent。这样更稳定、更便宜、更可控。

### 2. RAG 不存订单和库存

订单、库存是实时强结构化数据，走数据库或企业数据接口；RAG 存规则、政策、SOP。

### 3. Agent 工具区分实时数据和可缓存数据

库存不能缓存，规则可以短时间缓存。这体现业务正确性优先。

### 4. 有失败模式设计

订单不存在、LLM 不可用、Redis 不可用、工具超时、注入攻击都有对应处理。

### 5. 有评测和可观测闭环

不是“看起来能回答”，而是通过 trace、metrics、Ragas、DeepEval 做持续验证。

---

## 最不建议继续投入的方向

### 1. 不要继续无限加 Agent

Agent 数量多不等于系统强。面试官更关心什么时候不用 Agent、如何控制 Agent。

### 2. 不要把业务数据塞进 RAG

这会暴露对 RAG 和数据库边界理解不清。

### 3. 不要只展示聊天页面

聊天页面容易让项目看起来像套壳。一定要展示订单、库存、规则、Workflow、trace 的完整链路。

### 4. 不要假装是真实生产数据

合成数据可以用，但要明确说 synthetic data。诚实反而更专业。

---

## 最推荐先做的 6 件事

1. 生成一套能串起完整链路的合成数据。
2. 准备 3 个固定演示订单。
3. 增加一个面试演示看板。
4. 输出一份评测报告。
5. 写清楚 Demo 存储到生产数据库的演进方案。
6. 准备失败场景和架构取舍回答。

做好这 6 件事，这个项目在面试里会从“技术点很多的 AI Demo”变成“能解释业务、工程和生产边界的 AI 应用项目”。
